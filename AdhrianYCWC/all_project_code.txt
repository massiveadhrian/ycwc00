==================== ALL PROJECT CODE ====================

=== index.html ===

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description"
    content="DHRYZN â€” Your personal AI Study Mentor. Learn smarter with guided quizzes, practice exams, topic explanations, and progress tracking.">
  <meta name="theme-color" content="#0E1018">
  <meta name="author" content="DHRYZN">
  <title>DHRYZN â€” AI Study Mentor</title>

  <!-- Favicon -->
  <link rel="icon"
    href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='20' fill='%237C5CFF'/><text x='50' y='68' font-family='Arial' font-size='55' font-weight='bold' fill='white' text-anchor='middle'>D</text></svg>">

  <!-- Google Fonts â€” Inter -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- Stylesheets -->
  <link rel="stylesheet" href="css/variables.css">
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/sidebar.css">
  <link rel="stylesheet" href="css/dashboard.css">
  <link rel="stylesheet" href="css/subjects.css">
  <link rel="stylesheet" href="css/quiz.css">
  <link rel="stylesheet" href="css/exam.css">
  <link rel="stylesheet" href="css/progress.css">
  <link rel="stylesheet" href="css/history.css">
  <link rel="stylesheet" href="css/settings.css">
  <link rel="stylesheet" href="css/responsive.css">
</head>

<body>
  <div class="app-layout" id="app">
    <main class="main-content" id="main-content">
      <!-- SPA content rendered here by router -->
    </main>
  </div>

  <script type="module" src="js/app.js"></script>
</body>

</html>

=== css/base.css ===

/* ============================================
   DHRYZN â€” Base Styles & Animations
   ============================================ */

/* ---- Reset ---- */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-size: 16px;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-family);
  font-size: var(--text-body);
  font-weight: var(--weight-regular);
  line-height: var(--leading-normal);
  color: var(--color-text);
  background-color: var(--color-bg);
  overflow-x: hidden;
  min-height: 100vh;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  font-family: inherit;
  font-size: inherit;
  cursor: pointer;
  border: none;
  background: none;
  color: inherit;
  outline: none;
}

input, select, textarea {
  font-family: inherit;
  font-size: inherit;
  color: inherit;
  background: none;
  border: none;
  outline: none;
}

ul, ol {
  list-style: none;
}

img {
  max-width: 100%;
  display: block;
}

/* ---- Scrollbar ---- */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: rgba(124, 92, 255, 0.2);
  border-radius: var(--radius-full);
}

::-webkit-scrollbar-thumb:hover {
  background: rgba(124, 92, 255, 0.35);
}

/* ---- App Layout ---- */
.app-layout {
  display: flex;
  min-height: 100vh;
}

.main-content {
  flex: 1;
  margin-left: var(--sidebar-width);
  min-height: 100vh;
  position: relative;
}

.page-container {
  max-width: var(--content-max-width);
  margin: 0 auto;
  padding: var(--content-padding);
  padding-top: var(--space-10);
  padding-bottom: var(--space-16);
}

/* ---- Page Transitions ---- */
.page-enter {
  animation: pageEnter 0.4s ease forwards;
}

.page-exit {
  animation: pageExit 0.2s ease forwards;
}

/* ---- Typography Helpers ---- */
.text-hero {
  font-size: var(--text-hero);
  font-weight: var(--weight-bold);
  line-height: var(--leading-tight);
  letter-spacing: -0.02em;
}

.text-h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  line-height: var(--leading-tight);
  letter-spacing: -0.01em;
}

.text-h2 {
  font-size: var(--text-h2);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-tight);
}

.text-h3 {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-normal);
}

.text-secondary {
  color: var(--color-text-secondary);
}

.text-tertiary {
  color: var(--color-text-tertiary);
}

/* ---- Card Base ---- */
.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  transition: all var(--transition-base);
}

.card:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.card-interactive {
  cursor: pointer;
}

.card-interactive:hover {
  background: var(--color-surface-hover);
}

/* ---- Buttons ---- */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-3) var(--space-6);
  border-radius: var(--radius-full);
  font-weight: var(--weight-medium);
  font-size: var(--text-sm);
  transition: all var(--transition-base);
  white-space: nowrap;
  position: relative;
  overflow: hidden;
}

.btn-primary {
  background: var(--gradient-primary);
  color: white;
  box-shadow: var(--shadow-glow);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-glow-lg);
}

.btn-secondary {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  color: var(--color-text);
}

.btn-secondary:hover {
  border-color: var(--color-border-hover);
  background: var(--color-surface-hover);
  transform: translateY(-1px);
}

.btn-ghost {
  color: var(--color-text-secondary);
  padding: var(--space-2) var(--space-4);
}

.btn-ghost:hover {
  color: var(--color-text);
  background: var(--color-primary-subtle);
}

.btn-lg {
  padding: var(--space-4) var(--space-8);
  font-size: var(--text-body);
  border-radius: var(--radius-full);
}

.btn-icon {
  width: 40px;
  height: 40px;
  padding: 0;
  border-radius: var(--radius-md);
}

/* ---- Badges ---- */
.badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  padding: var(--space-1) var(--space-3);
  border-radius: var(--radius-full);
  font-size: var(--text-xs);
  font-weight: var(--weight-medium);
}

.badge-success {
  background: var(--color-success-bg);
  color: var(--color-success);
}

.badge-error {
  background: var(--color-error-bg);
  color: var(--color-error);
}

.badge-warning {
  background: var(--color-warning-bg);
  color: var(--color-warning);
}

.badge-info {
  background: var(--color-info-bg);
  color: var(--color-info);
}

/* ---- Form Elements ---- */
.input-field {
  width: 100%;
  padding: var(--space-3) var(--space-4);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  color: var(--color-text);
  font-size: var(--text-body);
  transition: all var(--transition-base);
}

.input-field::placeholder {
  color: var(--color-text-tertiary);
}

.input-field:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-glow);
}

.select-field {
  width: 100%;
  padding: var(--space-3) var(--space-4);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  color: var(--color-text);
  font-size: var(--text-body);
  transition: all var(--transition-base);
  cursor: pointer;
  appearance: none;
  background-image: url("data:image/svg+xml,%3Csvg width='12' height='8' viewBox='0 0 12 8' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1.5L6 6.5L11 1.5' stroke='%23A9A9B2' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
  background-repeat: no-repeat;
  background-position: right 12px center;
  padding-right: 36px;
}

.select-field:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-glow);
}

/* ---- Modal ---- */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
  z-index: var(--z-modal-overlay);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-6);
  animation: fadeIn 0.2s ease;
}

.modal {
  background: var(--color-surface-elevated);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
  width: 100%;
  max-width: 480px;
  z-index: var(--z-modal);
  animation: slideUp 0.3s ease;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-6);
}

.modal-title {
  font-size: var(--text-h2);
  font-weight: var(--weight-semibold);
}

.modal-close {
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-md);
  color: var(--color-text-secondary);
  transition: all var(--transition-fast);
}

.modal-close:hover {
  background: var(--color-primary-subtle);
  color: var(--color-text);
}

/* ---- Toast ---- */
.toast-container {
  position: fixed;
  bottom: var(--space-6);
  right: var(--space-6);
  z-index: var(--z-toast);
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.toast {
  background: var(--color-surface-elevated);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-4) var(--space-6);
  display: flex;
  align-items: center;
  gap: var(--space-3);
  min-width: 280px;
  animation: slideUp 0.3s ease;
  box-shadow: var(--shadow-lg);
}

.toast-success { border-left: 3px solid var(--color-success); }
.toast-error { border-left: 3px solid var(--color-error); }

/* ---- Animations ---- */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeOut {
  from { opacity: 1; }
  to { opacity: 0; }
}

@keyframes slideUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes slideDown {
  from { opacity: 0; transform: translateY(-10px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes slideInLeft {
  from { opacity: 0; transform: translateX(-20px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes pageEnter {
  from { opacity: 0; transform: translateY(12px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes pageExit {
  from { opacity: 1; transform: translateY(0); }
  to { opacity: 0; transform: translateY(-8px); }
}

@keyframes softGlow {
  0%, 100% { box-shadow: 0 0 15px rgba(124, 92, 255, 0.15); }
  50% { box-shadow: 0 0 25px rgba(124, 92, 255, 0.3); }
}

@keyframes typingDots {
  0%, 20% { opacity: 0.2; }
  50% { opacity: 1; }
  80%, 100% { opacity: 0.2; }
}

@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

@keyframes pulseRing {
  0% { transform: scale(0.8); opacity: 1; }
  100% { transform: scale(1.5); opacity: 0; }
}

@keyframes countUp {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ---- Animation Utilities ---- */
.animate-on-scroll {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}

.animate-on-scroll.visible {
  opacity: 1;
  transform: translateY(0);
}

.stagger-1 { transition-delay: 0.05s; }
.stagger-2 { transition-delay: 0.1s; }
.stagger-3 { transition-delay: 0.15s; }
.stagger-4 { transition-delay: 0.2s; }
.stagger-5 { transition-delay: 0.25s; }
.stagger-6 { transition-delay: 0.3s; }
.stagger-7 { transition-delay: 0.35s; }
.stagger-8 { transition-delay: 0.4s; }

/* ---- Typing Indicator ---- */
.typing-indicator {
  display: inline-flex;
  gap: 4px;
  padding: var(--space-3) var(--space-4);
}

.typing-indicator span {
  width: 7px;
  height: 7px;
  background: var(--color-primary-light);
  border-radius: 50%;
  animation: typingDots 1.4s infinite;
}

.typing-indicator span:nth-child(2) {
  animation-delay: 0.2s;
}

.typing-indicator span:nth-child(3) {
  animation-delay: 0.4s;
}

/* ---- Empty State ---- */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--space-16) var(--space-8);
  text-align: center;
}

.empty-state-icon {
  font-size: 3rem;
  margin-bottom: var(--space-4);
  opacity: 0.5;
}

.empty-state-title {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-2);
}

.empty-state-text {
  color: var(--color-text-secondary);
  max-width: 360px;
}

/* ---- Divider ---- */
.divider {
  height: 1px;
  background: var(--color-border);
  margin: var(--space-6) 0;
}

/* ---- Tab Bar ---- */
.tab-bar {
  display: flex;
  gap: var(--space-1);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-full);
  padding: var(--space-1);
}

.tab-item {
  padding: var(--space-2) var(--space-5);
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  color: var(--color-text-secondary);
  transition: all var(--transition-base);
  cursor: pointer;
}

.tab-item:hover {
  color: var(--color-text);
}

.tab-item.active {
  background: var(--color-primary);
  color: white;
}

/* ---- Progress Bar ---- */
.progress-bar {
  width: 100%;
  height: 6px;
  background: rgba(124, 92, 255, 0.1);
  border-radius: var(--radius-full);
  overflow: hidden;
}

.progress-bar-fill {
  height: 100%;
  background: var(--gradient-primary);
  border-radius: var(--radius-full);
  transition: width 0.6s ease;
}

/* ---- Skeleton Loading ---- */
.skeleton {
  background: linear-gradient(90deg, var(--color-surface) 25%, var(--color-surface-hover) 50%, var(--color-surface) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: var(--radius-md);
}

/* Mobile menu button */
.mobile-menu-btn {
  display: none;
  position: fixed;
  top: var(--space-4);
  left: var(--space-4);
  z-index: calc(var(--z-sidebar) + 1);
  width: 44px;
  height: 44px;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  align-items: center;
  justify-content: center;
  color: var(--color-text);
  font-size: 1.25rem;
}


=== css/dashboard.css ===

/* ============================================
   DHRYZN â€” Dashboard Styles
   ============================================ */

/* ---- Greeting ---- */
.dashboard-greeting {
  margin-bottom: var(--space-8);
  animation: fadeIn 0.5s ease;
}

.greeting-emoji {
  font-size: 2rem;
  margin-bottom: var(--space-2);
  display: inline-block;
}

.greeting-text {
  font-size: var(--text-hero);
  font-weight: var(--weight-bold);
  letter-spacing: -0.02em;
  margin-bottom: var(--space-2);
  line-height: var(--leading-tight);
}

.greeting-sub {
  font-size: var(--text-h3);
  color: var(--color-text-secondary);
  font-weight: var(--weight-regular);
}

/* ---- Quick Actions ---- */
.quick-actions {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-4);
  margin-bottom: var(--space-10);
}

.quick-action-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5) var(--space-5);
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  position: relative;
  overflow: hidden;
}

.quick-action-card::after {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--gradient-glow);
  opacity: 0;
  transition: opacity var(--transition-base);
}

.quick-action-card:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-3px);
  box-shadow: var(--shadow-glow);
}

.quick-action-card:hover::after {
  opacity: 1;
}

.quick-action-icon {
  width: 44px;
  height: 44px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.25rem;
  position: relative;
  z-index: 1;
}

.quick-action-icon.quiz { background: rgba(124, 92, 255, 0.15); color: #A78BFA; }
.quick-action-icon.explain { background: rgba(96, 165, 250, 0.15); color: #60A5FA; }
.quick-action-icon.exam { background: rgba(251, 191, 36, 0.15); color: #FBBF24; }
.quick-action-icon.review { background: rgba(52, 211, 153, 0.15); color: #34D399; }

.quick-action-title {
  font-size: var(--text-body);
  font-weight: var(--weight-semibold);
  position: relative;
  z-index: 1;
}

.quick-action-desc {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  position: relative;
  z-index: 1;
}

/* ---- Chat Interface ---- */
.chat-section {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.chat-header {
  padding: var(--space-5) var(--space-6);
  border-bottom: 1px solid var(--color-border);
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.chat-header-dot {
  width: 8px;
  height: 8px;
  background: var(--color-success);
  border-radius: 50%;
  animation: softGlow 2s infinite;
}

.chat-header-title {
  font-size: var(--text-body);
  font-weight: var(--weight-medium);
}

.chat-header-status {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.chat-messages {
  flex: 1;
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-5);
  max-height: 420px;
  overflow-y: auto;
  min-height: 200px;
}

.chat-message {
  display: flex;
  gap: var(--space-3);
  animation: slideUp 0.3s ease;
  max-width: 85%;
}

.chat-message.user {
  align-self: flex-end;
  flex-direction: row-reverse;
}

.chat-avatar {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-sm);
  flex-shrink: 0;
}

.chat-avatar.ai {
  background: var(--gradient-primary);
  color: white;
  font-weight: var(--weight-bold);
}

.chat-avatar.user {
  background: rgba(96, 165, 250, 0.2);
  color: #60A5FA;
}

.chat-bubble {
  padding: var(--space-3) var(--space-5);
  border-radius: var(--radius-lg);
  font-size: var(--text-body);
  line-height: var(--leading-relaxed);
}

.chat-message.ai .chat-bubble {
  background: var(--color-surface-elevated);
  border: 1px solid var(--color-border);
  border-top-left-radius: var(--radius-sm);
}

.chat-message.user .chat-bubble {
  background: var(--color-primary);
  color: white;
  border-top-right-radius: var(--radius-sm);
}

/* ---- Prompt Bar ---- */
.chat-input-area {
  padding: var(--space-4) var(--space-6) var(--space-6);
}

.prompt-bar {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-full);
  padding: var(--space-2) var(--space-2) var(--space-2) var(--space-5);
  transition: all var(--transition-base);
}

.prompt-bar:focus-within {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-glow), var(--shadow-glow);
}

.prompt-input {
  flex: 1;
  padding: var(--space-2) 0;
  font-size: var(--text-body);
  color: var(--color-text);
}

.prompt-input::placeholder {
  color: var(--color-text-tertiary);
}

.prompt-send-btn {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--gradient-primary);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--transition-base);
  flex-shrink: 0;
}

.prompt-send-btn:hover {
  transform: scale(1.05);
  box-shadow: var(--shadow-glow);
}

.prompt-send-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  transform: none;
}

/* ---- Suggested Prompts ---- */
.suggested-prompts {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
  margin-top: var(--space-3);
  padding: 0 var(--space-2);
}

.suggested-prompt-chip {
  padding: var(--space-2) var(--space-4);
  background: var(--color-primary-subtle);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-base);
}

.suggested-prompt-chip:hover {
  border-color: var(--color-primary);
  color: var(--color-text);
  background: rgba(124, 92, 255, 0.12);
}

/* ---- Stats Row ---- */
.dashboard-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-4);
  margin-bottom: var(--space-8);
}

.stat-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  display: flex;
  align-items: center;
  gap: var(--space-4);
}

.stat-icon {
  width: 48px;
  height: 48px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.3rem;
  flex-shrink: 0;
}

.stat-info {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.stat-value {
  font-size: var(--text-h2);
  font-weight: var(--weight-bold);
}

.stat-label {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}


=== css/exam.css ===

/* ============================================
   DHRYZN â€” Exam Styles
   ============================================ */

.exam-config {
  max-width: 680px;
  margin: 0 auto;
}

.exam-config-header {
  margin-bottom: var(--space-8);
}

.exam-config-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.exam-config-header p {
  color: var(--color-text-secondary);
}

/* ---- Exam Environment ---- */
.exam-environment {
  position: relative;
}

.exam-top-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-4) 0;
  margin-bottom: var(--space-6);
  border-bottom: 1px solid var(--color-border);
}

.exam-timer {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  font-variant-numeric: tabular-nums;
}

.exam-timer.warning {
  color: var(--color-warning);
}

.exam-timer.critical {
  color: var(--color-error);
  animation: softGlow 1s infinite;
}

.exam-timer-icon {
  font-size: 1.2rem;
}

.exam-info {
  display: flex;
  align-items: center;
  gap: var(--space-6);
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.exam-body {
  display: grid;
  grid-template-columns: 1fr 220px;
  gap: var(--space-6);
}

.exam-question-area {
  min-width: 0;
}

.exam-question-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
}

.exam-question-text {
  font-size: var(--text-h2);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-normal);
  margin-bottom: var(--space-6);
}

.exam-options {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.exam-option {
  padding: var(--space-4) var(--space-5);
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.exam-option:hover {
  border-color: var(--color-border-hover);
  background: var(--color-surface-hover);
}

.exam-option.selected {
  border-color: var(--color-primary);
  background: var(--color-primary-subtle);
}

.exam-option-letter {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-md);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-sm);
  font-weight: var(--weight-semibold);
  flex-shrink: 0;
}

.exam-option.selected .exam-option-letter {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: white;
}

/* ---- Question Navigation Panel ---- */
.exam-nav-panel {
  position: sticky;
  top: var(--space-6);
}

.exam-nav-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
}

.exam-nav-title {
  font-size: var(--text-sm);
  font-weight: var(--weight-semibold);
  color: var(--color-text-secondary);
  margin-bottom: var(--space-4);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.exam-nav-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: var(--space-2);
  margin-bottom: var(--space-5);
}

.exam-nav-btn {
  width: 36px;
  height: 36px;
  border-radius: var(--radius-md);
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.exam-nav-btn:hover {
  border-color: var(--color-border-hover);
}

.exam-nav-btn.current {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: white;
}

.exam-nav-btn.answered {
  background: var(--color-primary-subtle);
  border-color: rgba(124, 92, 255, 0.3);
  color: var(--color-primary-light);
}

.exam-nav-btn.bookmarked {
  position: relative;
}

.exam-nav-btn.bookmarked::after {
  content: 'â˜…';
  position: absolute;
  top: -4px;
  right: -4px;
  font-size: 10px;
  color: var(--color-warning);
}

.exam-nav-legend {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
  padding-top: var(--space-4);
  border-top: 1px solid var(--color-border);
}

.exam-legend-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-xs);
  color: var(--color-text-secondary);
}

.exam-legend-dot {
  width: 10px;
  height: 10px;
  border-radius: 3px;
  flex-shrink: 0;
}

.exam-question-actions {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: var(--space-6);
}

.bookmark-btn {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-base);
  background: transparent;
  border: 1px solid var(--color-border);
}

.bookmark-btn:hover {
  border-color: var(--color-warning);
  color: var(--color-warning);
}

.bookmark-btn.active {
  background: var(--color-warning-bg);
  border-color: var(--color-warning);
  color: var(--color-warning);
}

/* ---- Review Before Submit ---- */
.exam-review {
  max-width: 680px;
  margin: 0 auto;
}

.exam-review h2 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.exam-review-summary {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-4);
  margin: var(--space-6) 0;
}

.exam-review-stat {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  text-align: center;
}

.exam-review-stat-value {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
}

.exam-review-stat-label {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  margin-top: var(--space-1);
}

.exam-review-actions {
  display: flex;
  gap: var(--space-3);
  justify-content: center;
  margin-top: var(--space-6);
}

/* ---- Exam Results ---- */
.exam-results {
  max-width: 800px;
  margin: 0 auto;
}

.exam-results-header {
  text-align: center;
  margin-bottom: var(--space-8);
}

.exam-results-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.exam-results-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-5);
  margin-bottom: var(--space-8);
}

.exam-result-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
}

.exam-result-card h3 {
  font-size: var(--text-body);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-4);
  color: var(--color-text-secondary);
}

.readiness-gauge {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-3);
}

.readiness-gauge canvas {
  width: 120px;
  height: 120px;
}

.readiness-label {
  font-size: var(--text-h2);
  font-weight: var(--weight-bold);
}

.weak-topics-list {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.weak-topic-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--space-2) 0;
}

.weak-topic-name {
  font-size: var(--text-body);
  font-weight: var(--weight-medium);
}

.weak-topic-score {
  font-size: var(--text-sm);
  color: var(--color-error);
  font-weight: var(--weight-semibold);
}


=== css/history.css ===

/* ============================================
   DHRYZN â€” History Styles
   ============================================ */

.history-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-6);
  flex-wrap: wrap;
  gap: var(--space-4);
}

.history-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
}

/* ---- Tabs ---- */
.history-tabs {
  margin-bottom: var(--space-6);
}

/* ---- Filters ---- */
.history-filters {
  display: flex;
  gap: var(--space-3);
  margin-bottom: var(--space-6);
  flex-wrap: wrap;
}

.history-search {
  flex: 1;
  min-width: 200px;
  position: relative;
}

.history-search-icon {
  position: absolute;
  left: var(--space-4);
  top: 50%;
  transform: translateY(-50%);
  color: var(--color-text-tertiary);
  font-size: var(--text-body);
  pointer-events: none;
}

.history-search .input-field {
  padding-left: 40px;
}

.history-filter-select {
  width: 160px;
}

/* ---- History List ---- */
.history-list {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.history-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5) var(--space-6);
  display: flex;
  align-items: center;
  gap: var(--space-5);
  transition: all var(--transition-base);
  cursor: pointer;
}

.history-card:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-1px);
  box-shadow: var(--shadow-sm);
}

.history-card-icon {
  width: 44px;
  height: 44px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  flex-shrink: 0;
}

.history-card-icon.quiz {
  background: rgba(124, 92, 255, 0.12);
  color: var(--color-primary-light);
}

.history-card-icon.exam {
  background: rgba(251, 191, 36, 0.12);
  color: var(--color-warning);
}

.history-card-info {
  flex: 1;
  min-width: 0;
}

.history-card-title {
  font-size: var(--text-body);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-1);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.history-card-meta {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.history-card-meta span {
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

.history-card-score {
  font-size: var(--text-h3);
  font-weight: var(--weight-bold);
  flex-shrink: 0;
}

.history-card-score.high {
  color: var(--color-success);
}

.history-card-score.medium {
  color: var(--color-warning);
}

.history-card-score.low {
  color: var(--color-error);
}

.history-card-actions {
  display: flex;
  gap: var(--space-2);
  flex-shrink: 0;
}


=== css/progress.css ===

/* ============================================
   DHRYZN â€” Progress Styles
   ============================================ */

.progress-page-header {
  margin-bottom: var(--space-8);
}

.progress-page-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.progress-page-header p {
  color: var(--color-text-secondary);
}

/* ---- Stats Grid ---- */
.progress-stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-4);
  margin-bottom: var(--space-8);
}

.progress-stat-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  transition: all var(--transition-base);
}

.progress-stat-card:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-2px);
}

.progress-stat-icon {
  font-size: 1.5rem;
  margin-bottom: var(--space-3);
}

.progress-stat-value {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-1);
  font-variant-numeric: tabular-nums;
}

.progress-stat-label {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

/* ---- Chart Cards ---- */
.progress-charts {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-5);
  margin-bottom: var(--space-8);
}

.progress-chart-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-6);
}

.progress-chart-card.full-width {
  grid-column: 1 / -1;
}

.progress-chart-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-5);
}

.progress-chart-title {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
}

.progress-chart-subtitle {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.chart-container {
  width: 100%;
  position: relative;
}

.chart-container canvas {
  width: 100% !important;
  height: auto !important;
}

/* ---- Mastery Gauge ---- */
.mastery-section {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: var(--space-4);
  padding: var(--space-4) 0;
}

.mastery-canvas-wrap {
  position: relative;
  width: 160px;
  height: 160px;
}

.mastery-canvas-wrap canvas {
  width: 100%;
  height: 100%;
}

.mastery-value {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.mastery-percent {
  font-size: var(--text-hero);
  font-weight: var(--weight-bold);
}

.mastery-label {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

/* ---- Topics Bars ---- */
.topics-list {
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.topic-item {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.topic-item-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.topic-name {
  font-size: var(--text-body);
  font-weight: var(--weight-medium);
}

.topic-percent {
  font-size: var(--text-sm);
  font-weight: var(--weight-semibold);
  color: var(--color-text-secondary);
}

.topic-bar {
  height: 8px;
  background: rgba(124, 92, 255, 0.1);
  border-radius: var(--radius-full);
  overflow: hidden;
}

.topic-bar-fill {
  height: 100%;
  border-radius: var(--radius-full);
  transition: width 0.8s ease;
}

.topic-bar-fill.strong {
  background: var(--color-success);
}

.topic-bar-fill.medium {
  background: var(--color-warning);
}

.topic-bar-fill.weak {
  background: var(--color-error);
}

/* ---- Recommended Action ---- */
.recommended-action {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-6);
  display: flex;
  align-items: center;
  gap: var(--space-5);
  transition: all var(--transition-base);
  cursor: pointer;
}

.recommended-action:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-2px);
  box-shadow: var(--shadow-glow);
}

.recommended-icon {
  width: 56px;
  height: 56px;
  border-radius: var(--radius-lg);
  background: rgba(124, 92, 255, 0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  flex-shrink: 0;
}

.recommended-content {
  flex: 1;
}

.recommended-title {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-1);
}

.recommended-desc {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.recommended-arrow {
  font-size: 1.25rem;
  color: var(--color-text-tertiary);
}

/* ---- Exam Readiness ---- */
.readiness-section {
  display: flex;
  align-items: center;
  gap: var(--space-8);
}

.readiness-chart-wrap {
  position: relative;
  width: 120px;
  height: 120px;
  flex-shrink: 0;
}

.readiness-chart-wrap canvas {
  width: 100%;
  height: 100%;
}

.readiness-chart-value {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
}

.readiness-details {
  flex: 1;
}

.readiness-status {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-2);
}

.readiness-desc {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  line-height: var(--leading-relaxed);
}


=== css/quiz.css ===

/* ============================================
   DHRYZN â€” Quiz Styles
   ============================================ */

/* ---- Quiz Config ---- */
.quiz-config {
  max-width: 680px;
  margin: 0 auto;
}

.quiz-config-header {
  margin-bottom: var(--space-8);
}

.quiz-config-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.quiz-config-header p {
  color: var(--color-text-secondary);
}

.quiz-config-form {
  display: flex;
  flex-direction: column;
  gap: var(--space-5);
}

.form-group {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.form-group label {
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  color: var(--color-text-secondary);
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-4);
}

/* ---- Question Type Selector ---- */
.question-type-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-3);
}

.question-type-option {
  padding: var(--space-3) var(--space-4);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all var(--transition-base);
  text-align: center;
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
}

.question-type-option:hover {
  border-color: var(--color-border-hover);
}

.question-type-option.selected {
  border-color: var(--color-primary);
  background: var(--color-primary-subtle);
  color: var(--color-primary-light);
}

.quiz-config-actions {
  display: flex;
  gap: var(--space-3);
  justify-content: flex-end;
  margin-top: var(--space-4);
}

/* ---- Quiz Taking ---- */
.quiz-taking {
  max-width: 740px;
  margin: 0 auto;
}

.quiz-progress-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-3);
}

.quiz-progress-text {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  font-weight: var(--weight-medium);
}

.quiz-progress-bar {
  margin-bottom: var(--space-8);
}

.quiz-question-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
  margin-bottom: var(--space-6);
  animation: slideUp 0.3s ease;
}

.quiz-question-number {
  font-size: var(--text-sm);
  color: var(--color-primary-light);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-3);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.quiz-question-text {
  font-size: var(--text-h2);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-normal);
  margin-bottom: var(--space-6);
}

/* ---- Answer Options ---- */
.quiz-options {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.quiz-option {
  padding: var(--space-4) var(--space-5);
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  align-items: center;
  gap: var(--space-3);
  font-size: var(--text-body);
}

.quiz-option:hover:not(.disabled) {
  border-color: var(--color-border-hover);
  background: var(--color-surface-hover);
}

.quiz-option.selected {
  border-color: var(--color-primary);
  background: var(--color-primary-subtle);
}

.quiz-option.correct {
  border-color: var(--color-success);
  background: var(--color-success-bg);
}

.quiz-option.incorrect {
  border-color: var(--color-error);
  background: var(--color-error-bg);
}

.quiz-option.disabled {
  cursor: not-allowed;
  opacity: 0.7;
}

.quiz-option-letter {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-md);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-sm);
  font-weight: var(--weight-semibold);
  flex-shrink: 0;
  transition: all var(--transition-base);
}

.quiz-option.selected .quiz-option-letter {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: white;
}

.quiz-option.correct .quiz-option-letter {
  background: var(--color-success);
  border-color: var(--color-success);
  color: white;
}

.quiz-option.incorrect .quiz-option-letter {
  background: var(--color-error);
  border-color: var(--color-error);
  color: white;
}

/* ---- Feedback ---- */
.quiz-feedback {
  margin-top: var(--space-5);
  padding: var(--space-4) var(--space-5);
  border-radius: var(--radius-lg);
  animation: slideUp 0.3s ease;
  font-size: var(--text-body);
  line-height: var(--leading-relaxed);
}

.quiz-feedback.correct {
  background: var(--color-success-bg);
  border: 1px solid rgba(52, 211, 153, 0.2);
}

.quiz-feedback.incorrect {
  background: var(--color-error-bg);
  border: 1px solid rgba(248, 113, 113, 0.2);
}

.quiz-feedback-title {
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-2);
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.quiz-nav-actions {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* Short answer / Essay */
.quiz-text-input {
  width: 100%;
  padding: var(--space-4);
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  color: var(--color-text);
  font-size: var(--text-body);
  line-height: var(--leading-relaxed);
  resize: vertical;
  min-height: 60px;
  transition: all var(--transition-base);
}

.quiz-text-input:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-glow);
}

/* ---- Quiz Results ---- */
.quiz-results {
  max-width: 680px;
  margin: 0 auto;
  text-align: center;
}

.quiz-results-score {
  margin: var(--space-8) 0;
}

.quiz-score-circle {
  width: 140px;
  height: 140px;
  margin: 0 auto var(--space-4);
  position: relative;
}

.quiz-score-circle canvas {
  width: 100%;
  height: 100%;
}

.quiz-score-value {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-hero);
  font-weight: var(--weight-bold);
}

.quiz-results-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-4);
  margin: var(--space-8) 0;
}

.quiz-result-stat {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
}

.quiz-result-stat-value {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-1);
}

.quiz-result-stat-label {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.quiz-results-actions {
  display: flex;
  gap: var(--space-3);
  justify-content: center;
  margin-top: var(--space-6);
}

/* ---- Question Review List ---- */
.quiz-review-list {
  margin-top: var(--space-8);
  text-align: left;
}

.quiz-review-list h3 {
  font-size: var(--text-h2);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-5);
}

.quiz-review-item {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  margin-bottom: var(--space-3);
}

.quiz-review-item.correct {
  border-left: 3px solid var(--color-success);
}

.quiz-review-item.incorrect {
  border-left: 3px solid var(--color-error);
}

.quiz-review-question {
  font-weight: var(--weight-medium);
  margin-bottom: var(--space-2);
}

.quiz-review-answer {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}


=== css/responsive.css ===

/* ============================================
   DHRYZN â€” Responsive Styles
   ============================================ */

/* ---- Tablet (1024px) ---- */
@media (max-width: 1024px) {
  :root {
    --content-padding: var(--space-6);
  }

  .quick-actions {
    grid-template-columns: repeat(2, 1fr);
  }

  .progress-stats-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .progress-charts {
    grid-template-columns: 1fr;
  }

  .exam-body {
    grid-template-columns: 1fr;
  }

  .exam-nav-panel {
    position: static;
  }

  .exam-nav-card {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
    align-items: flex-start;
  }

  .exam-nav-grid {
    margin-bottom: 0;
  }

  .exam-results-grid {
    grid-template-columns: 1fr;
  }

  .subject-actions {
    grid-template-columns: repeat(2, 1fr);
  }

  .dashboard-stats {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* ---- Small Tablet (768px) ---- */
@media (max-width: 768px) {
  .mobile-menu-btn {
    display: flex;
  }

  .sidebar {
    transform: translateX(-100%);
  }

  .sidebar.open {
    transform: translateX(0);
  }

  .sidebar-overlay.visible {
    display: block;
  }

  .main-content {
    margin-left: 0;
  }

  .page-container {
    padding-top: calc(var(--space-10) + 44px);
  }

  .quick-actions {
    grid-template-columns: repeat(2, 1fr);
  }

  .subjects-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .subject-actions {
    grid-template-columns: 1fr;
  }

  .quiz-results-stats {
    grid-template-columns: repeat(3, 1fr);
  }

  .exam-review-summary {
    grid-template-columns: repeat(3, 1fr);
  }

  .history-card {
    flex-wrap: wrap;
  }

  .history-card-actions {
    width: 100%;
    justify-content: flex-end;
  }

  .settings-item {
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-3);
  }

  .readiness-section {
    flex-direction: column;
    text-align: center;
  }

  .form-row {
    grid-template-columns: 1fr;
  }

  .dashboard-stats {
    grid-template-columns: 1fr;
  }

  .chat-messages {
    max-height: 350px;
  }
}

/* ---- Mobile (480px) ---- */
@media (max-width: 480px) {
  :root {
    --content-padding: var(--space-4);
    --text-hero: 1.75rem;
    --text-h1: 1.5rem;
    --text-h2: 1.25rem;
  }

  .quick-actions {
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
  }

  .quick-action-card {
    padding: var(--space-4);
  }

  .quick-action-desc {
    display: none;
  }

  .subjects-grid {
    grid-template-columns: 1fr;
  }

  .progress-stats-grid {
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
  }

  .quiz-results-stats {
    grid-template-columns: 1fr;
  }

  .exam-review-summary {
    grid-template-columns: 1fr;
  }

  .history-filters {
    flex-direction: column;
  }

  .history-search {
    min-width: 100%;
  }

  .history-filter-select {
    width: 100%;
  }

  .prompt-bar {
    border-radius: var(--radius-lg);
  }

  .suggested-prompts {
    flex-direction: column;
  }

  .suggested-prompt-chip {
    text-align: center;
  }

  .chat-message {
    max-width: 95%;
  }

  .quiz-question-card {
    padding: var(--space-5);
  }

  .exam-question-card {
    padding: var(--space-5);
  }

  .quiz-nav-actions {
    flex-direction: column;
    gap: var(--space-3);
  }

  .quiz-nav-actions .btn {
    width: 100%;
  }

  .quiz-results-actions {
    flex-direction: column;
  }

  .quiz-results-actions .btn {
    width: 100%;
  }

  .exam-question-actions {
    flex-direction: column;
    gap: var(--space-3);
  }

  .tab-bar {
    width: 100%;
  }

  .tab-item {
    flex: 1;
    text-align: center;
  }

  .question-type-grid {
    grid-template-columns: 1fr;
  }
}


=== css/settings.css ===

/* ============================================
   DHRYZN â€” Settings Styles
   ============================================ */

.settings-header {
  margin-bottom: var(--space-8);
}

.settings-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.settings-header p {
  color: var(--color-text-secondary);
}

.settings-sections {
  display: flex;
  flex-direction: column;
  gap: var(--space-6);
  max-width: 680px;
}

.settings-section {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-6);
}

.settings-section-title {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  margin-bottom: var(--space-5);
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.settings-section-title span {
  font-size: 1.1rem;
}

.settings-group {
  display: flex;
  flex-direction: column;
  gap: var(--space-5);
}

.settings-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-6);
}

.settings-item-info {
  flex: 1;
}

.settings-item-label {
  font-size: var(--text-body);
  font-weight: var(--weight-medium);
  margin-bottom: var(--space-1);
}

.settings-item-desc {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
}

.settings-item-control {
  flex-shrink: 0;
}

/* ---- Toggle Switch ---- */
.toggle-switch {
  position: relative;
  width: 48px;
  height: 26px;
  cursor: pointer;
}

.toggle-switch input {
  display: none;
}

.toggle-track {
  position: absolute;
  inset: 0;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-full);
  transition: all var(--transition-base);
}

.toggle-track::after {
  content: '';
  position: absolute;
  top: 3px;
  left: 3px;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--color-text-secondary);
  transition: all var(--transition-base);
}

.toggle-switch input:checked + .toggle-track {
  background: var(--color-primary);
  border-color: var(--color-primary);
}

.toggle-switch input:checked + .toggle-track::after {
  transform: translateX(22px);
  background: white;
}

/* ---- Color Picker ---- */
.color-options {
  display: flex;
  gap: var(--space-2);
}

.color-option {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  cursor: pointer;
  border: 2px solid transparent;
  transition: all var(--transition-base);
  position: relative;
}

.color-option:hover {
  transform: scale(1.1);
}

.color-option.active {
  border-color: white;
  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px currentColor;
}

/* ---- Slider ---- */
.settings-slider {
  width: 160px;
  -webkit-appearance: none;
  appearance: none;
  height: 6px;
  background: rgba(124, 92, 255, 0.15);
  border-radius: var(--radius-full);
  outline: none;
}

.settings-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--color-primary);
  cursor: pointer;
  border: 2px solid var(--color-bg);
  box-shadow: 0 0 6px rgba(124, 92, 255, 0.4);
}

/* ---- Select in Settings ---- */
.settings-select {
  width: 180px;
}

/* ---- Reset / About ---- */
.settings-reset-btn {
  margin-top: var(--space-2);
}

.settings-about {
  text-align: center;
  padding: var(--space-4) 0;
}

.settings-about-logo {
  font-size: var(--text-h2);
  font-weight: var(--weight-bold);
  background: var(--gradient-primary);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: var(--space-2);
}

.settings-about-version {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  margin-bottom: var(--space-3);
}

.settings-about-desc {
  font-size: var(--text-sm);
  color: var(--color-text-tertiary);
  max-width: 400px;
  margin: 0 auto;
  line-height: var(--leading-relaxed);
}


=== css/sidebar.css ===

/* ============================================
   DHRYZN â€” Sidebar Styles
   ============================================ */

.sidebar {
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
  width: var(--sidebar-width);
  background: var(--color-surface);
  border-right: 1px solid var(--color-border);
  display: flex;
  flex-direction: column;
  z-index: var(--z-sidebar);
  transition: transform var(--transition-base);
}

/* ---- Brand ---- */
.sidebar-brand {
  padding: var(--space-6) var(--space-6) var(--space-8);
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.sidebar-logo {
  width: 38px;
  height: 38px;
  background: var(--gradient-primary);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: var(--weight-bold);
  font-size: var(--text-h3);
  color: white;
  box-shadow: var(--shadow-glow);
  flex-shrink: 0;
}

.sidebar-brand-text {
  font-size: var(--text-h2);
  font-weight: var(--weight-bold);
  letter-spacing: 0.06em;
  background: var(--gradient-primary);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* ---- Navigation ---- */
.sidebar-nav {
  flex: 1;
  padding: 0 var(--space-3);
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.sidebar-nav-item {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-3) var(--space-4);
  border-radius: var(--radius-md);
  color: var(--color-text-secondary);
  font-size: var(--text-body);
  font-weight: var(--weight-medium);
  transition: all var(--transition-base);
  cursor: pointer;
  position: relative;
  overflow: hidden;
}

.sidebar-nav-item::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%) scaleY(0);
  width: 3px;
  height: 60%;
  background: var(--color-primary);
  border-radius: 0 var(--radius-full) var(--radius-full) 0;
  transition: transform var(--transition-base);
}

.sidebar-nav-item:hover {
  color: var(--color-text);
  background: var(--color-primary-subtle);
}

.sidebar-nav-item.active {
  color: var(--color-text);
  background: var(--color-primary-subtle);
}

.sidebar-nav-item.active::before {
  transform: translateY(-50%) scaleY(1);
}

.sidebar-nav-icon {
  font-size: 1.15rem;
  width: 24px;
  text-align: center;
  flex-shrink: 0;
}

.sidebar-nav-label {
  white-space: nowrap;
}

/* ---- Footer ---- */
.sidebar-footer {
  padding: var(--space-4) var(--space-6);
  border-top: 1px solid var(--color-border);
}

.sidebar-footer-text {
  font-size: var(--text-xs);
  color: var(--color-text-tertiary);
  text-align: center;
}

/* ---- Mobile Overlay ---- */
.sidebar-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: calc(var(--z-sidebar) - 1);
}


=== css/subjects.css ===

/* ============================================
   DHRYZN â€” Subjects Styles
   ============================================ */

.subjects-header {
  margin-bottom: var(--space-8);
}

.subjects-header h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
  margin-bottom: var(--space-2);
}

.subjects-header p {
  color: var(--color-text-secondary);
}

/* ---- Subject Grid ---- */
.subjects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: var(--space-5);
}

.subject-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  cursor: pointer;
  transition: all var(--transition-base);
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.subject-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: var(--gradient-primary);
  opacity: 0;
  transition: opacity var(--transition-base);
}

.subject-card:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-3px);
  box-shadow: var(--shadow-glow);
}

.subject-card:hover::before {
  opacity: 1;
}

.subject-icon {
  width: 52px;
  height: 52px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
}

.subject-name {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
}

.subject-meta {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

/* ---- Add Card ---- */
.subject-card.add-card {
  border-style: dashed;
  border-color: var(--color-border);
  align-items: center;
  justify-content: center;
  text-align: center;
  min-height: 160px;
}

.subject-card.add-card:hover {
  border-color: var(--color-primary);
  background: var(--color-primary-subtle);
}

.add-card-icon {
  width: 52px;
  height: 52px;
  border-radius: 50%;
  background: var(--color-primary-subtle);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  color: var(--color-primary);
  margin-bottom: var(--space-2);
}

.add-card-text {
  font-size: var(--text-body);
  font-weight: var(--weight-medium);
  color: var(--color-text-secondary);
}

/* ---- Subject Detail ---- */
.subject-detail-header {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  margin-bottom: var(--space-8);
}

.subject-detail-back {
  width: 40px;
  height: 40px;
  border-radius: var(--radius-md);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all var(--transition-base);
  flex-shrink: 0;
}

.subject-detail-back:hover {
  background: var(--color-surface-hover);
  border-color: var(--color-border-hover);
}

.subject-detail-info h1 {
  font-size: var(--text-h1);
  font-weight: var(--weight-bold);
}

.subject-detail-info p {
  color: var(--color-text-secondary);
  font-size: var(--text-sm);
}

.subject-actions {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-5);
  margin-bottom: var(--space-10);
}

.subject-action-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: var(--space-8) var(--space-6);
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: var(--space-4);
  position: relative;
  overflow: hidden;
}

.subject-action-card::after {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--gradient-glow);
  opacity: 0;
  transition: opacity var(--transition-base);
}

.subject-action-card:hover {
  transform: translateY(-4px);
  border-color: var(--color-border-hover);
  box-shadow: var(--shadow-glow);
}

.subject-action-card:hover::after {
  opacity: 1;
}

.subject-action-icon {
  width: 60px;
  height: 60px;
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  position: relative;
  z-index: 1;
}

.subject-action-title {
  font-size: var(--text-h3);
  font-weight: var(--weight-semibold);
  position: relative;
  z-index: 1;
}

.subject-action-desc {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  position: relative;
  z-index: 1;
}

/* ---- Subject Modal ---- */
.subject-modal-input {
  margin-bottom: var(--space-4);
}

.subject-modal-input label {
  display: block;
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  margin-bottom: var(--space-2);
  color: var(--color-text-secondary);
}

.subject-modal-actions {
  display: flex;
  gap: var(--space-3);
  justify-content: flex-end;
  margin-top: var(--space-6);
}


=== css/variables.css ===

/* ============================================
   DHRYZN â€” Design Tokens & Variables
   ============================================ */

:root {
  /* ---- Color Palette ---- */
  --color-primary: #7C5CFF;
  --color-primary-rgb: 124, 92, 255;
  --color-primary-light: #9B82FF;
  --color-primary-dark: #5A3ED9;
  --color-primary-glow: rgba(124, 92, 255, 0.25);
  --color-primary-subtle: rgba(124, 92, 255, 0.08);

  --color-bg: #0E1018;
  --color-bg-rgb: 14, 16, 24;
  --color-surface: #151522;
  --color-surface-rgb: 21, 21, 34;
  --color-surface-hover: #1C1C30;
  --color-surface-elevated: #1E1E32;

  --color-border: rgba(124, 92, 255, 0.12);
  --color-border-hover: rgba(124, 92, 255, 0.25);
  --color-border-active: rgba(124, 92, 255, 0.4);

  --color-text: #FFFFFF;
  --color-text-secondary: #A9A9B2;
  --color-text-tertiary: #6B6B78;
  --color-text-inverse: #0E1018;

  /* Semantic Colors */
  --color-success: #34D399;
  --color-success-bg: rgba(52, 211, 153, 0.1);
  --color-error: #F87171;
  --color-error-bg: rgba(248, 113, 113, 0.1);
  --color-warning: #FBBF24;
  --color-warning-bg: rgba(251, 191, 36, 0.1);
  --color-info: #60A5FA;
  --color-info-bg: rgba(96, 165, 250, 0.1);

  /* Gradient Accents */
  --gradient-primary: linear-gradient(135deg, #7C5CFF 0%, #A78BFA 100%);
  --gradient-surface: linear-gradient(180deg, rgba(124, 92, 255, 0.06) 0%, transparent 100%);
  --gradient-glow: radial-gradient(ellipse at center, rgba(124, 92, 255, 0.15) 0%, transparent 70%);

  /* ---- Typography ---- */
  --font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', monospace;

  --text-hero: 2.25rem;       /* 36px */
  --text-h1: 1.75rem;         /* 28px */
  --text-h2: 1.375rem;        /* 22px */
  --text-h3: 1.125rem;        /* 18px */
  --text-body: 0.9375rem;     /* 15px */
  --text-sm: 0.8125rem;       /* 13px */
  --text-xs: 0.75rem;         /* 12px */

  --weight-regular: 400;
  --weight-medium: 500;
  --weight-semibold: 600;
  --weight-bold: 700;

  --leading-tight: 1.25;
  --leading-normal: 1.5;
  --leading-relaxed: 1.75;

  /* ---- Spacing ---- */
  --space-1: 0.25rem;   /* 4px */
  --space-2: 0.5rem;    /* 8px */
  --space-3: 0.75rem;   /* 12px */
  --space-4: 1rem;      /* 16px */
  --space-5: 1.25rem;   /* 20px */
  --space-6: 1.5rem;    /* 24px */
  --space-8: 2rem;      /* 32px */
  --space-10: 2.5rem;   /* 40px */
  --space-12: 3rem;     /* 48px */
  --space-16: 4rem;     /* 64px */

  /* ---- Border Radius ---- */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;
  --radius-2xl: 28px;
  --radius-full: 9999px;

  /* ---- Shadows ---- */
  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 8px 30px rgba(0, 0, 0, 0.5);
  --shadow-glow: 0 0 20px rgba(124, 92, 255, 0.2);
  --shadow-glow-lg: 0 0 40px rgba(124, 92, 255, 0.3);

  /* ---- Transitions ---- */
  --transition-fast: 150ms ease;
  --transition-base: 250ms ease;
  --transition-slow: 400ms ease;
  --transition-spring: 500ms cubic-bezier(0.34, 1.56, 0.64, 1);

  /* ---- Layout ---- */
  --sidebar-width: 260px;
  --sidebar-collapsed: 72px;
  --header-height: 0px;
  --content-max-width: 1100px;
  --content-padding: var(--space-8);

  /* ---- Z-index ---- */
  --z-base: 1;
  --z-sidebar: 100;
  --z-modal-overlay: 500;
  --z-modal: 600;
  --z-toast: 700;
  --z-tooltip: 800;
}


=== js/app.js ===

// ============================================
// DHRYZN â€” Main Application Entry
// ============================================

import { Store } from './store.js';
import { Router } from './router.js';
import { renderSidebar } from './components/sidebar.js';
import { renderDashboard } from './components/dashboard.js';
import { renderSubjects } from './components/subjects.js';
import { renderSubjectDetail } from './components/subjectDetail.js';
import { renderQuiz } from './components/quiz.js';
import { renderExplainTopic } from './components/explainTopic.js';
import { renderExam } from './components/exam.js';
import { renderProgress } from './components/progress.js';
import { renderHistory } from './components/history.js';
import { renderSettings } from './components/settings.js';

// Initialize store
const store = new Store();

// Wait for DOM
document.addEventListener('DOMContentLoaded', () => {
  const appLayout = document.getElementById('app');
  const mainContent = document.getElementById('main-content');

  // Initialize router
  const router = new Router(mainContent);

  // Render sidebar
  renderSidebar(appLayout, router);

  // Register routes
  router.register('dashboard', (el, params) => renderDashboard(el, params, store, router));
  router.register('subjects', (el, params) => renderSubjects(el, params, store, router));
  router.register('subject', (el, params) => renderSubjectDetail(el, params, store, router));
  router.register('quiz', (el, params) => renderQuiz(el, params, store, router));
  router.register('explain', (el, params) => renderExplainTopic(el, params, store, router));
  router.register('exam', (el, params) => renderExam(el, params, store, router));
  router.register('progress', (el, params) => renderProgress(el, params, store, router));
  router.register('history', (el, params) => renderHistory(el, params, store, router));
  router.register('settings', (el, params) => renderSettings(el, params, store, router));

  // Start router
  router.start();
});


=== js/router.js ===

// ============================================
// DHRYZN â€” SPA Router
// ============================================

export class Router {
  constructor(contentEl) {
    this.routes = {};
    this.contentEl = contentEl;
    this.currentRoute = null;
    this.currentCleanup = null;

    window.addEventListener('hashchange', () => this.handleRoute());
  }

  register(path, renderFn) {
    this.routes[path] = renderFn;
  }

  handleRoute() {
    const hash = window.location.hash.slice(1) || 'dashboard';
    const [route, ...params] = hash.split('/');

    if (this.currentRoute === hash) return;
    this.currentRoute = hash;

    // Run cleanup for previous page
    if (this.currentCleanup && typeof this.currentCleanup === 'function') {
      this.currentCleanup();
    }

    const renderFn = this.routes[route];
    if (renderFn) {
      // Page transition
      this.contentEl.classList.remove('page-enter');
      this.contentEl.classList.add('page-exit');

      setTimeout(() => {
        this.contentEl.innerHTML = '';
        this.currentCleanup = renderFn(this.contentEl, params) || null;
        this.contentEl.classList.remove('page-exit');
        this.contentEl.classList.add('page-enter');

        // Update sidebar active state
        document.querySelectorAll('.sidebar-nav-item').forEach(item => {
          item.classList.toggle('active', item.dataset.route === route);
        });

        // Scroll to top
        this.contentEl.scrollTop = 0;
        window.scrollTo(0, 0);
      }, 150);
    }
  }

  navigate(path) {
    window.location.hash = path;
  }

  start() {
    this.handleRoute();
  }
}


=== js/store.js ===

// ============================================
// DHRYZN â€” State Management Store
// ============================================

export class Store {
  constructor() {
    this.listeners = {};
    this.state = this.loadState();
  }

  getDefaultState() {
    return {
      subjects: [
        { id: 'math', name: 'Mathematics', icon: 'ðŸ“', color: '#7C5CFF', questionsAnswered: 47 },
        { id: 'physics', name: 'Physics', icon: 'âš›ï¸', color: '#60A5FA', questionsAnswered: 32 },
        { id: 'chemistry', name: 'Chemistry', icon: 'ðŸ§ª', color: '#34D399', questionsAnswered: 28 },
        { id: 'biology', name: 'Biology', icon: 'ðŸ§¬', color: '#F87171', questionsAnswered: 19 },
        { id: 'english', name: 'English', icon: 'ðŸ“–', color: '#FBBF24', questionsAnswered: 35 },
        { id: 'cs', name: 'Computer Science', icon: 'ðŸ’»', color: '#A78BFA', questionsAnswered: 41 },
        { id: 'history', name: 'History', icon: 'ðŸ›ï¸', color: '#FB923C', questionsAnswered: 15 }
      ],
      quizHistory: [
        { id: 'q1', subject: 'Mathematics', topic: 'Quadratic Equations', date: '2026-07-31', duration: '12 min', score: 85, total: 10, correct: 8, type: 'quiz', status: 'completed' },
        { id: 'q2', subject: 'Physics', topic: 'Newton\'s Laws of Motion', date: '2026-07-30', duration: '15 min', score: 70, total: 10, correct: 7, type: 'quiz', status: 'completed' },
        { id: 'q3', subject: 'Chemistry', topic: 'Chemical Bonding', date: '2026-07-29', duration: '10 min', score: 90, total: 10, correct: 9, type: 'quiz', status: 'completed' },
        { id: 'q4', subject: 'English', topic: 'Shakespearean Literature', date: '2026-07-28', duration: '8 min', score: 60, total: 10, correct: 6, type: 'quiz', status: 'completed' },
        { id: 'q5', subject: 'Computer Science', topic: 'Data Structures', date: '2026-07-27', duration: '18 min', score: 95, total: 10, correct: 9, type: 'quiz', status: 'completed' }
      ],
      examHistory: [
        { id: 'e1', subject: 'Mathematics', topic: 'Algebra & Calculus Midterm', date: '2026-07-30', duration: '45 min', score: 78, total: 25, correct: 19, type: 'exam', status: 'completed' },
        { id: 'e2', subject: 'Physics', topic: 'Mechanics Final Exam', date: '2026-07-28', duration: '60 min', score: 82, total: 30, correct: 24, type: 'exam', status: 'completed' },
        { id: 'e3', subject: 'Computer Science', topic: 'Algorithms Practice Test', date: '2026-07-25', duration: '50 min', score: 88, total: 20, correct: 17, type: 'exam', status: 'completed' }
      ],
      progress: {
        streak: 7,
        totalQuestions: 217,
        averageAccuracy: 81,
        studyTimeMinutes: 845,
        mastery: 72,
        weeklyActivity: [45, 30, 60, 25, 55, 40, 50],
        strongTopics: [
          { name: 'Data Structures', percent: 95 },
          { name: 'Chemical Bonding', percent: 90 },
          { name: 'Quadratic Equations', percent: 88 }
        ],
        weakTopics: [
          { name: 'Shakespearean Literature', percent: 42 },
          { name: 'Thermodynamics', percent: 48 },
          { name: 'Integration', percent: 55 }
        ],
        examReadiness: 74,
        examTrend: [65, 70, 68, 75, 78, 82, 74]
      },
      settings: {
        theme: 'dark',
        accentColor: '#7C5CFF',
        language: 'English',
        defaultDifficulty: 'Medium',
        preferredQuestionType: 'Multiple Choice',
        explanationStyle: 'Step-by-Step',
        dailyGoal: 30,
        timerEnabled: true
      },
      chatMessages: []
    };
  }

  loadState() {
    try {
      const saved = localStorage.getItem('dhryzn_state');
      if (saved) {
        const parsed = JSON.parse(saved);
        // Merge with defaults to ensure new fields are present
        const defaults = this.getDefaultState();
        return { ...defaults, ...parsed };
      }
    } catch (e) {
      console.warn('Failed to load state:', e);
    }
    return this.getDefaultState();
  }

  saveState() {
    try {
      localStorage.setItem('dhryzn_state', JSON.stringify(this.state));
    } catch (e) {
      console.warn('Failed to save state:', e);
    }
  }

  get(key) {
    return this.state[key];
  }

  set(key, value) {
    this.state[key] = value;
    this.saveState();
    this.emit(key, value);
  }

  update(key, updater) {
    this.state[key] = updater(this.state[key]);
    this.saveState();
    this.emit(key, this.state[key]);
  }

  on(event, callback) {
    if (!this.listeners[event]) {
      this.listeners[event] = [];
    }
    this.listeners[event].push(callback);
    return () => {
      this.listeners[event] = this.listeners[event].filter(cb => cb !== callback);
    };
  }

  emit(event, data) {
    if (this.listeners[event]) {
      this.listeners[event].forEach(cb => cb(data));
    }
  }

  reset() {
    this.state = this.getDefaultState();
    this.saveState();
    Object.keys(this.listeners).forEach(key => this.emit(key, this.state[key]));
  }

  addQuizToHistory(quiz) {
    this.update('quizHistory', history => [quiz, ...history]);
    this.update('progress', p => ({
      ...p,
      totalQuestions: p.totalQuestions + quiz.total,
      averageAccuracy: Math.round(
        (p.averageAccuracy * (p.totalQuestions) + quiz.score * quiz.total / 100) /
        (p.totalQuestions + quiz.total) * 100 / 100
      )
    }));
  }

  addExamToHistory(exam) {
    this.update('examHistory', history => [exam, ...history]);
  }

  addSubject(subject) {
    this.update('subjects', subjects => [...subjects, subject]);
  }
}


=== js/components/dashboard.js ===

// ============================================
// DHRYZN â€” Dashboard Component
// ============================================

import { getGreeting } from '../utils/greeting.js';
import { getAIResponse } from '../utils/mockAI.js';
import { staggerReveal } from '../utils/animations.js';
import { suggestedPrompts } from '../data/sampleData.js';

export function renderDashboard(container, params, store, router) {
  const greeting = getGreeting();
  const progress = store.get('progress');
  const messages = store.get('chatMessages') || [];

  container.innerHTML = `
    <div class="page-container">
      <!-- Greeting -->
      <div class="dashboard-greeting">
        <div class="greeting-emoji">${greeting.emoji}</div>
        <h1 class="greeting-text">${greeting.text}!</h1>
        <p class="greeting-sub">${greeting.motivational}</p>
      </div>

      <!-- Quick Stats -->
      <div class="dashboard-stats">
        <div class="stat-card animate-item">
          <div class="stat-icon" style="background: rgba(124, 92, 255, 0.12); color: #A78BFA;">ðŸ”¥</div>
          <div class="stat-info">
            <div class="stat-value">${progress.streak}</div>
            <div class="stat-label">Day Streak</div>
          </div>
        </div>
        <div class="stat-card animate-item">
          <div class="stat-icon" style="background: rgba(52, 211, 153, 0.12); color: #34D399;">ðŸ“Š</div>
          <div class="stat-info">
            <div class="stat-value">${progress.averageAccuracy}%</div>
            <div class="stat-label">Avg. Accuracy</div>
          </div>
        </div>
        <div class="stat-card animate-item">
          <div class="stat-icon" style="background: rgba(96, 165, 250, 0.12); color: #60A5FA;">ðŸ“</div>
          <div class="stat-info">
            <div class="stat-value">${progress.totalQuestions}</div>
            <div class="stat-label">Questions Answered</div>
          </div>
        </div>
      </div>

      <!-- Quick Actions -->
      <div class="quick-actions">
        <div class="quick-action-card animate-item" data-action="quiz">
          <div class="quick-action-icon quiz">ðŸ“</div>
          <div class="quick-action-title">Generate Quiz</div>
          <div class="quick-action-desc">Test your knowledge with custom quizzes</div>
        </div>
        <div class="quick-action-card animate-item" data-action="explain">
          <div class="quick-action-icon explain">ðŸ’¡</div>
          <div class="quick-action-title">Explain Topic</div>
          <div class="quick-action-desc">Get clear explanations on any subject</div>
        </div>
        <div class="quick-action-card animate-item" data-action="exam">
          <div class="quick-action-icon exam">ðŸŽ¯</div>
          <div class="quick-action-title">Practice Exam</div>
          <div class="quick-action-desc">Simulate real exam conditions</div>
        </div>
        <div class="quick-action-card animate-item" data-action="review">
          <div class="quick-action-icon review">ðŸ”</div>
          <div class="quick-action-title">Review Mistakes</div>
          <div class="quick-action-desc">Learn from your past errors</div>
        </div>
      </div>

      <!-- AI Chat -->
      <div class="chat-section">
        <div class="chat-header">
          <div class="chat-header-dot"></div>
          <span class="chat-header-title">DHRYZN AI Mentor</span>
          <span class="chat-header-status">â€¢ Online</span>
        </div>

        <div class="chat-messages" id="chat-messages">
          ${messages.length === 0 ? `
            <div class="chat-message ai">
              <div class="chat-avatar ai">D</div>
              <div class="chat-bubble">
                Hey there! ðŸ‘‹ I'm DHRYZN, your personal study mentor. I'm here to help you learn, practice, and grow. What would you like to work on today?
              </div>
            </div>
          ` : messages.map(msg => `
            <div class="chat-message ${msg.role}">
              <div class="chat-avatar ${msg.role}">${msg.role === 'ai' ? 'D' : 'ðŸ‘¤'}</div>
              <div class="chat-bubble">${formatMessage(msg.content)}</div>
            </div>
          `).join('')}
        </div>

        <div class="chat-input-area">
          <div class="prompt-bar">
            <input type="text" class="prompt-input" id="prompt-input" placeholder="What would you like to learn today?" autocomplete="off">
            <button class="prompt-send-btn" id="prompt-send" aria-label="Send message">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
            </button>
          </div>
          <div class="suggested-prompts" id="suggested-prompts">
            ${suggestedPrompts.map(p => `
              <span class="suggested-prompt-chip">${p}</span>
            `).join('')}
          </div>
        </div>
      </div>
    </div>
  `;

  // Animate items
  staggerReveal(container, '.animate-item', 60);

  // Chat functionality
  const chatMessages = container.querySelector('#chat-messages');
  const promptInput = container.querySelector('#prompt-input');
  const sendBtn = container.querySelector('#prompt-send');
  const suggestedPromptsEl = container.querySelector('#suggested-prompts');

  function scrollToBottom() {
    chatMessages.scrollTop = chatMessages.scrollHeight;
  }

  scrollToBottom();

  async function sendMessage(text) {
    if (!text.trim()) return;

    // Add user message
    const userMsg = { role: 'user', content: text };
    addMessageToUI(userMsg);
    store.update('chatMessages', msgs => [...msgs, userMsg]);

    promptInput.value = '';
    sendBtn.disabled = true;

    // Show typing indicator
    const typingEl = document.createElement('div');
    typingEl.className = 'chat-message ai';
    typingEl.innerHTML = `
      <div class="chat-avatar ai">D</div>
      <div class="chat-bubble">
        <div class="typing-indicator">
          <span></span><span></span><span></span>
        </div>
      </div>
    `;
    chatMessages.appendChild(typingEl);
    scrollToBottom();

    // Get AI response
    const response = await getAIResponse(text);

    // Remove typing indicator and add response
    chatMessages.removeChild(typingEl);
    const aiMsg = { role: 'ai', content: response };
    addMessageToUI(aiMsg);
    store.update('chatMessages', msgs => [...msgs, aiMsg]);

    sendBtn.disabled = false;
  }

  function addMessageToUI(msg) {
    const msgEl = document.createElement('div');
    msgEl.className = `chat-message ${msg.role}`;
    msgEl.innerHTML = `
      <div class="chat-avatar ${msg.role}">${msg.role === 'ai' ? 'D' : 'ðŸ‘¤'}</div>
      <div class="chat-bubble">${formatMessage(msg.content)}</div>
    `;
    chatMessages.appendChild(msgEl);
    scrollToBottom();
  }

  // Event listeners
  sendBtn.addEventListener('click', () => sendMessage(promptInput.value));

  promptInput.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      sendMessage(promptInput.value);
    }
  });

  suggestedPromptsEl.addEventListener('click', (e) => {
    const chip = e.target.closest('.suggested-prompt-chip');
    if (chip) {
      sendMessage(chip.textContent);
    }
  });

  // Quick action handlers
  container.querySelectorAll('.quick-action-card').forEach(card => {
    card.addEventListener('click', () => {
      const action = card.dataset.action;
      switch (action) {
        case 'quiz': router.navigate('quiz'); break;
        case 'explain': router.navigate('explain'); break;
        case 'exam': router.navigate('exam'); break;
        case 'review': router.navigate('history'); break;
      }
    });
  });
}

function formatMessage(text) {
  // Simple markdown-like formatting
  return text
    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
    .replace(/\n/g, '<br>')
    .replace(/â€¢ /g, '&bull; ')
    .replace(/â†’ /g, 'â†’ ');
}


=== js/components/exam.js ===

// ============================================
// DHRYZN â€” Practice Exam Component
// ============================================

import { generateQuizQuestions } from '../utils/mockAI.js';
import { staggerReveal } from '../utils/animations.js';
import { drawCircularProgress } from '../utils/charts.js';
import { difficultyLevels, gradeLevels } from '../data/sampleData.js';

export function renderExam(container, params, store, router) {
  const subjectId = params[0];
  const subjects = store.get('subjects');
  const subject = subjectId ? subjects.find(s => s.id === subjectId) : null;

  // Shared cleanup reference so timer can be cleared on page exit
  const state = { cleanup: null };
  renderExamConfig(container, subject, store, router, state);

  return () => {
    if (state.cleanup) state.cleanup();
  };
}

function renderExamConfig(container, subject, store, router, state) {
  container.innerHTML = `
    <div class="page-container">
      <div class="exam-config">
        <div class="exam-config-header">
          <h1>ðŸŽ¯ Practice Exam</h1>
          <p>Simulate a real exam environment with a timer and no instant feedback.</p>
        </div>

        <div class="quiz-config-form">
          <div class="form-group animate-item">
            <label>Subject / Topic</label>
            <input type="text" class="input-field" id="exam-topic" placeholder="e.g., Physics â€” Mechanics" value="${subject ? subject.name : ''}">
          </div>

          <div class="form-row animate-item">
            <div class="form-group">
              <label>Duration (minutes)</label>
              <select class="select-field" id="exam-duration">
                <option value="15">15 minutes</option>
                <option value="30" selected>30 minutes</option>
                <option value="45">45 minutes</option>
                <option value="60">60 minutes</option>
                <option value="90">90 minutes</option>
              </select>
            </div>
            <div class="form-group">
              <label>Number of Questions</label>
              <select class="select-field" id="exam-count">
                <option value="10">10 Questions</option>
                <option value="15">15 Questions</option>
                <option value="20" selected>20 Questions</option>
                <option value="25">25 Questions</option>
                <option value="30">30 Questions</option>
              </select>
            </div>
          </div>

          <div class="form-row animate-item">
            <div class="form-group">
              <label>Difficulty</label>
              <select class="select-field" id="exam-difficulty">
                ${difficultyLevels.map(d => `<option value="${d}" ${d === 'Medium' ? 'selected' : ''}>${d}</option>`).join('')}
              </select>
            </div>
            <div class="form-group">
              <label>Grade Level</label>
              <select class="select-field" id="exam-grade">
                ${gradeLevels.map(g => `<option value="${g}">${g}</option>`).join('')}
              </select>
            </div>
          </div>

          <div class="quiz-config-actions animate-item">
            <button class="btn btn-secondary" id="exam-back-btn">Cancel</button>
            <button class="btn btn-primary btn-lg" id="exam-start-btn">ðŸš€ Start Exam</button>
          </div>
        </div>
      </div>
    </div>
  `;

  staggerReveal(container, '.animate-item', 60);

  container.querySelector('#exam-back-btn').addEventListener('click', () => {
    window.history.back();
  });

  container.querySelector('#exam-start-btn').addEventListener('click', () => {
    const topic = container.querySelector('#exam-topic').value.trim() || 'General';
    const duration = parseInt(container.querySelector('#exam-duration').value);
    const count = parseInt(container.querySelector('#exam-count').value);
    const difficulty = container.querySelector('#exam-difficulty').value;

    const questions = generateQuizQuestions(topic, count, 'Multiple Choice', difficulty);
    state.cleanup = renderExamEnvironment(container, questions, topic, duration, store, router);
  });
}

function renderExamEnvironment(container, questions, topic, durationMinutes, store, router) {
  let currentQuestion = 0;
  let answers = new Array(questions.length).fill(null);
  let bookmarked = new Array(questions.length).fill(false);
  let timeLeft = durationMinutes * 60; // seconds
  let timerInterval = null;

  function formatTime(sec) {
    const m = Math.floor(sec / 60);
    const s = sec % 60;
    return `${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;
  }

  function getTimerClass() {
    const percent = timeLeft / (durationMinutes * 60);
    if (percent <= 0.1) return 'critical';
    if (percent <= 0.25) return 'warning';
    return '';
  }

  function startTimer() {
    timerInterval = setInterval(() => {
      timeLeft--;
      const timerEl = container.querySelector('#exam-timer');
      if (timerEl) {
        timerEl.textContent = formatTime(timeLeft);
        timerEl.parentElement.className = `exam-timer ${getTimerClass()}`;
      }
      if (timeLeft <= 0) {
        clearInterval(timerInterval);
        submitExamResults(container, questions, answers, topic, durationMinutes, timeLeft, store, router);
      }
    }, 1000);
  }

  function render() {
    const q = questions[currentQuestion];
    const answeredCount = answers.filter(a => a !== null).length;

    container.innerHTML = `
      <div class="page-container">
        <div class="exam-environment">
          <div class="exam-top-bar">
            <div class="exam-timer ${getTimerClass()}">
              <span class="exam-timer-icon">â±</span>
              <span id="exam-timer">${formatTime(timeLeft)}</span>
            </div>
            <div class="exam-info">
              <span>${topic}</span>
              <span>${answeredCount}/${questions.length} answered</span>
            </div>
          </div>

          <div class="exam-body">
            <div class="exam-question-area">
              <div class="exam-question-card">
                <div class="quiz-question-number">Question ${currentQuestion + 1}</div>
                <div class="exam-question-text">${q.question}</div>

                <div class="exam-options">
                  ${q.options.map((opt, i) => `
                    <div class="exam-option ${answers[currentQuestion] === i ? 'selected' : ''}" data-index="${i}">
                      <div class="exam-option-letter">${String.fromCharCode(65 + i)}</div>
                      <span>${opt}</span>
                    </div>
                  `).join('')}
                </div>

                <div class="exam-question-actions">
                  <button class="bookmark-btn ${bookmarked[currentQuestion] ? 'active' : ''}" id="bookmark-btn">
                    ${bookmarked[currentQuestion] ? 'â˜…' : 'â˜†'} ${bookmarked[currentQuestion] ? 'Bookmarked' : 'Bookmark'}
                  </button>
                  <div style="display: flex; gap: var(--space-3);">
                    <button class="btn btn-secondary" id="exam-prev" ${currentQuestion === 0 ? 'disabled style="opacity:0.4"' : ''}>â† Prev</button>
                    ${currentQuestion < questions.length - 1 ? `
                      <button class="btn btn-primary" id="exam-next">Next â†’</button>
                    ` : `
                      <button class="btn btn-primary" id="exam-review-btn">ðŸ“‹ Review & Submit</button>
                    `}
                  </div>
                </div>
              </div>
            </div>

            <div class="exam-nav-panel">
              <div class="exam-nav-card">
                <div class="exam-nav-title">Questions</div>
                <div class="exam-nav-grid">
                  ${questions.map((_, i) => {
                    let cls = 'exam-nav-btn';
                    if (i === currentQuestion) cls += ' current';
                    else if (answers[i] !== null) cls += ' answered';
                    if (bookmarked[i]) cls += ' bookmarked';
                    return `<button class="${cls}" data-qi="${i}">${i + 1}</button>`;
                  }).join('')}
                </div>
                <div class="exam-nav-legend">
                  <div class="exam-legend-item">
                    <div class="exam-legend-dot" style="background: var(--color-primary);"></div>
                    <span>Current</span>
                  </div>
                  <div class="exam-legend-item">
                    <div class="exam-legend-dot" style="background: var(--color-primary-subtle); border: 1px solid rgba(124,92,255,0.3);"></div>
                    <span>Answered</span>
                  </div>
                  <div class="exam-legend-item">
                    <div class="exam-legend-dot" style="background: var(--color-bg); border: 1px solid var(--color-border);"></div>
                    <span>Unanswered</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    `;

    // Option selection
    container.querySelectorAll('.exam-option').forEach(opt => {
      opt.addEventListener('click', () => {
        answers[currentQuestion] = parseInt(opt.dataset.index);
        container.querySelectorAll('.exam-option').forEach(o => o.classList.remove('selected'));
        opt.classList.add('selected');
        opt.querySelector('.exam-option-letter').style.background = 'var(--color-primary)';
        opt.querySelector('.exam-option-letter').style.color = 'white';
      });
    });

    // Bookmark
    container.querySelector('#bookmark-btn').addEventListener('click', () => {
      bookmarked[currentQuestion] = !bookmarked[currentQuestion];
      render();
    });

    // Navigation
    const prevBtn = container.querySelector('#exam-prev');
    if (prevBtn && currentQuestion > 0) {
      prevBtn.addEventListener('click', () => { currentQuestion--; render(); });
    }

    const nextBtn = container.querySelector('#exam-next');
    if (nextBtn) {
      nextBtn.addEventListener('click', () => { currentQuestion++; render(); });
    }

    // Question nav grid
    container.querySelectorAll('.exam-nav-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        currentQuestion = parseInt(btn.dataset.qi);
        render();
      });
    });

    // Review & Submit
    const reviewBtn = container.querySelector('#exam-review-btn');
    if (reviewBtn) {
      reviewBtn.addEventListener('click', () => {
        clearInterval(timerInterval);
        renderExamReview(container, questions, answers, bookmarked, topic, durationMinutes, timeLeft, store, router, () => {
          startTimer();
          render();
        });
      });
    }
  }

  startTimer();
  render();

  return () => {
    if (timerInterval) clearInterval(timerInterval);
  };
}

function renderExamReview(container, questions, answers, bookmarked, topic, durationMinutes, timeLeft, store, router, goBack) {
  const answeredCount = answers.filter(a => a !== null).length;
  const unanswered = questions.length - answeredCount;
  const bookmarkedCount = bookmarked.filter(b => b).length;

  container.innerHTML = `
    <div class="page-container">
      <div class="exam-review">
        <h2>ðŸ“‹ Review Before Submitting</h2>
        <p class="text-secondary" style="margin-top: var(--space-2);">Take a moment to review your progress before submitting.</p>

        <div class="exam-review-summary">
          <div class="exam-review-stat">
            <div class="exam-review-stat-value" style="color: var(--color-success);">${answeredCount}</div>
            <div class="exam-review-stat-label">Answered</div>
          </div>
          <div class="exam-review-stat">
            <div class="exam-review-stat-value" style="color: var(--color-warning);">${unanswered}</div>
            <div class="exam-review-stat-label">Unanswered</div>
          </div>
          <div class="exam-review-stat">
            <div class="exam-review-stat-value" style="color: var(--color-info);">${bookmarkedCount}</div>
            <div class="exam-review-stat-label">Bookmarked</div>
          </div>
        </div>

        ${unanswered > 0 ? `
          <div style="background: var(--color-warning-bg); border: 1px solid rgba(251,191,36,0.2); border-radius: var(--radius-lg); padding: var(--space-4) var(--space-5); margin-bottom: var(--space-5);">
            âš ï¸ You have <strong>${unanswered} unanswered</strong> question${unanswered > 1 ? 's' : ''}. Are you sure you want to submit?
          </div>
        ` : ''}

        <div class="exam-review-actions">
          <button class="btn btn-secondary" id="review-back">â† Go Back</button>
          <button class="btn btn-primary btn-lg" id="review-submit">âœ… Submit Exam</button>
        </div>
      </div>
    </div>
  `;

  container.querySelector('#review-back').addEventListener('click', goBack);

  container.querySelector('#review-submit').addEventListener('click', () => {
    submitExamResults(container, questions, answers, topic, durationMinutes, timeLeft, store, router);
  });
}

function submitExamResults(container, questions, answers, topic, durationMinutes, timeLeft, store, router) {
  let correct = 0;
  questions.forEach((q, i) => { if (answers[i] === q.correct) correct++; });
  const score = Math.round((correct / questions.length) * 100);
  const timeUsed = (durationMinutes * 60) - timeLeft;
  const minutes = Math.floor(timeUsed / 60);

  // Save
  store.addExamToHistory({
    id: 'e-' + Date.now(),
    subject: topic,
    topic: topic + ' Practice Exam',
    date: new Date().toISOString().split('T')[0],
    duration: `${minutes} min`,
    score,
    total: questions.length,
    correct,
    type: 'exam',
    status: 'completed'
  });

  // Weak topics (mock)
  const weakTopics = [
    { name: 'Advanced Concepts', score: Math.max(20, score - 30) },
    { name: 'Application Problems', score: Math.max(25, score - 20) },
    { name: 'Theoretical Foundations', score: Math.min(95, score + 10) }
  ];

  container.innerHTML = `
    <div class="page-container">
      <div class="exam-results">
        <div class="exam-results-header">
          <h1>${score >= 80 ? 'ðŸŽ‰ Outstanding!' : score >= 60 ? 'ðŸ‘ Good Effort!' : 'ðŸ’ª Room to Grow!'}</h1>
          <p class="text-secondary">${score >= 80 ? 'You\'re well-prepared!' : score >= 60 ? 'Keep practicing to improve!' : 'Focus on your weak areas and try again!'}</p>
        </div>

        <div class="exam-results-grid">
          <div class="exam-result-card">
            <h3>ðŸ“Š Performance</h3>
            <div class="readiness-gauge">
              <canvas id="exam-score-chart"></canvas>
              <div class="readiness-label">${score}%</div>
            </div>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-4); margin-top: var(--space-4);">
              <div style="text-align: center;">
                <div style="font-size: var(--text-h2); font-weight: 700; color: var(--color-success);">${correct}</div>
                <div style="font-size: var(--text-sm); color: var(--color-text-secondary);">Correct</div>
              </div>
              <div style="text-align: center;">
                <div style="font-size: var(--text-h2); font-weight: 700; color: var(--color-error);">${questions.length - correct}</div>
                <div style="font-size: var(--text-sm); color: var(--color-text-secondary);">Incorrect</div>
              </div>
            </div>
          </div>

          <div class="exam-result-card">
            <h3>â± Time Analysis</h3>
            <div style="display: flex; flex-direction: column; gap: var(--space-4); padding: var(--space-4) 0;">
              <div>
                <div style="font-size: var(--text-sm); color: var(--color-text-secondary); margin-bottom: var(--space-1);">Time Used</div>
                <div style="font-size: var(--text-h2); font-weight: 700;">${minutes} min</div>
              </div>
              <div>
                <div style="font-size: var(--text-sm); color: var(--color-text-secondary); margin-bottom: var(--space-1);">Avg per Question</div>
                <div style="font-size: var(--text-h2); font-weight: 700;">${Math.round(timeUsed / questions.length)}s</div>
              </div>
              <div>
                <div style="font-size: var(--text-sm); color: var(--color-text-secondary); margin-bottom: var(--space-1);">Time Remaining</div>
                <div style="font-size: var(--text-h2); font-weight: 700;">${Math.floor(timeLeft / 60)}m ${timeLeft % 60}s</div>
              </div>
            </div>
          </div>

          <div class="exam-result-card">
            <h3>ðŸ“ˆ Topic Analysis</h3>
            <div class="weak-topics-list">
              ${weakTopics.map(t => `
                <div class="weak-topic-item">
                  <span class="weak-topic-name">${t.name}</span>
                  <span class="weak-topic-score" style="color: ${t.score >= 70 ? 'var(--color-success)' : t.score >= 50 ? 'var(--color-warning)' : 'var(--color-error)'};">${t.score}%</span>
                </div>
              `).join('')}
            </div>
          </div>

          <div class="exam-result-card">
            <h3>ðŸŽ¯ Exam Readiness</h3>
            <div class="readiness-section" style="flex-direction: column; align-items: center;">
              <div class="readiness-chart-wrap">
                <canvas id="readiness-chart"></canvas>
                <div class="readiness-chart-value">${Math.round(score * 0.9)}%</div>
              </div>
              <div style="text-align: center; margin-top: var(--space-3);">
                <div class="readiness-status">${score >= 80 ? 'Exam Ready' : score >= 60 ? 'Almost There' : 'Keep Practicing'}</div>
                <div class="readiness-desc">${score >= 80 ? 'You\'re well prepared for the exam!' : score >= 60 ? 'A few more practice sessions will help.' : 'Focus on weak areas and retake.'}</div>
              </div>
            </div>
          </div>
        </div>

        <div style="display: flex; gap: var(--space-3); justify-content: center; margin-top: var(--space-6);">
          <button class="btn btn-secondary" id="exam-retake">ðŸ”„ Retake Exam</button>
          <button class="btn btn-primary" id="exam-dashboard">ðŸ  Dashboard</button>
        </div>
      </div>
    </div>
  `;

  // Draw charts
  const scoreCanvas = container.querySelector('#exam-score-chart');
  if (scoreCanvas) drawCircularProgress(scoreCanvas, score, { size: 120, lineWidth: 10 });

  const readinessCanvas = container.querySelector('#readiness-chart');
  if (readinessCanvas) drawCircularProgress(readinessCanvas, Math.round(score * 0.9), { size: 120, lineWidth: 10 });

  container.querySelector('#exam-retake').addEventListener('click', () => router.navigate('exam'));
  container.querySelector('#exam-dashboard').addEventListener('click', () => router.navigate('dashboard'));
}


=== js/components/explainTopic.js ===

// ============================================
// DHRYZN â€” Explain Topic Component
// ============================================

import { generateExplanation } from '../utils/mockAI.js';
import { staggerReveal, typewriterEffect } from '../utils/animations.js';
import { gradeLevels, explanationStyles } from '../data/sampleData.js';

export function renderExplainTopic(container, params, store, router) {
  const subjectId = params[0];
  const subjects = store.get('subjects');
  const subject = subjectId ? subjects.find(s => s.id === subjectId) : null;

  container.innerHTML = `
    <div class="page-container">
      <div class="quiz-config">
        <div class="quiz-config-header">
          <h1>ðŸ’¡ Explain Topic</h1>
          <p>Get clear, detailed explanations on any topic â€” just the way you prefer.</p>
        </div>

        <div class="quiz-config-form">
          <div class="form-group animate-item">
            <label>Topic</label>
            <input type="text" class="input-field" id="explain-topic" placeholder="e.g., Photosynthesis, Derivatives, World War II" value="${subject ? subject.name : ''}">
          </div>

          <div class="form-row animate-item">
            <div class="form-group">
              <label>Grade Level</label>
              <select class="select-field" id="explain-grade">
                ${gradeLevels.map(g => `<option value="${g}">${g}</option>`).join('')}
              </select>
            </div>
            <div class="form-group">
              <label>Explanation Style</label>
              <select class="select-field" id="explain-style">
                ${explanationStyles.map(s => `<option value="${s}">${s}</option>`).join('')}
              </select>
            </div>
          </div>

          <div class="quiz-config-actions animate-item">
            <button class="btn btn-secondary" id="explain-back-btn">Cancel</button>
            <button class="btn btn-primary btn-lg" id="explain-go-btn">ðŸ’¡ Get Explanation</button>
          </div>
        </div>

        <!-- Explanation Result -->
        <div id="explanation-result" style="display: none; margin-top: var(--space-8);">
          <div class="divider"></div>
          <div style="background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-xl); padding: var(--space-6); margin-top: var(--space-6);">
            <div style="display: flex; align-items: center; gap: var(--space-3); margin-bottom: var(--space-4);">
              <div style="width: 32px; height: 32px; background: var(--gradient-primary); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; color: white; font-weight: 600; font-size: 0.85rem;">D</div>
              <span style="font-weight: 600;">DHRYZN explains:</span>
            </div>
            <div id="explanation-content" style="line-height: 1.75; white-space: pre-wrap;"></div>
          </div>

          <!-- Follow-up Actions -->
          <div style="display: flex; gap: var(--space-3); margin-top: var(--space-5); flex-wrap: wrap;">
            <button class="btn btn-secondary" id="followup-quiz">ðŸ“ Generate Quiz</button>
            <button class="btn btn-secondary" id="followup-exam">ðŸŽ¯ Practice Exam</button>
            <button class="btn btn-secondary" id="followup-another">ðŸ’¡ Explain Another</button>
          </div>
        </div>
      </div>
    </div>
  `;

  staggerReveal(container, '.animate-item', 60);

  container.querySelector('#explain-back-btn').addEventListener('click', () => {
    window.history.back();
  });

  container.querySelector('#explain-go-btn').addEventListener('click', async () => {
    const topic = container.querySelector('#explain-topic').value.trim() || 'General Topic';
    const style = container.querySelector('#explain-style').value;

    const resultEl = container.querySelector('#explanation-result');
    const contentEl = container.querySelector('#explanation-content');

    // Show loading
    resultEl.style.display = 'block';
    contentEl.innerHTML = `
      <div class="typing-indicator">
        <span></span><span></span><span></span>
      </div>
    `;

    // Simulate delay
    await new Promise(r => setTimeout(r, 1200));

    const explanation = generateExplanation(topic, style);
    contentEl.textContent = '';
    await typewriterEffect(contentEl, explanation, 12);

    // Format bold text after typewriter
    contentEl.innerHTML = contentEl.textContent
      .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
      .replace(/\n/g, '<br>')
      .replace(/â€¢ /g, '&bull; ')
      .replace(/â†’ /g, 'â†’ ');
  });

  // Follow-up buttons
  container.querySelector('#followup-quiz')?.addEventListener('click', () => {
    router.navigate('quiz');
  });
  container.querySelector('#followup-exam')?.addEventListener('click', () => {
    router.navigate('exam');
  });
  container.querySelector('#followup-another')?.addEventListener('click', () => {
    container.querySelector('#explain-topic').value = '';
    container.querySelector('#explanation-result').style.display = 'none';
    container.querySelector('#explain-topic').focus();
  });
}


=== js/components/history.js ===

// ============================================
// DHRYZN â€” History Component
// ============================================

import { staggerReveal } from '../utils/animations.js';

export function renderHistory(container, params, store, router) {
  let activeTab = 'quiz';
  let searchQuery = '';
  let filterSubject = '';

  function render() {
    const quizHistory = store.get('quizHistory') || [];
    const examHistory = store.get('examHistory') || [];
    const items = activeTab === 'quiz' ? quizHistory : examHistory;

    // Apply filters
    let filtered = items;
    if (searchQuery) {
      const q = searchQuery.toLowerCase();
      filtered = filtered.filter(item =>
        item.topic.toLowerCase().includes(q) ||
        item.subject.toLowerCase().includes(q)
      );
    }
    if (filterSubject) {
      filtered = filtered.filter(item => item.subject === filterSubject);
    }

    // Get unique subjects for filter
    const allSubjects = [...new Set([...quizHistory, ...examHistory].map(i => i.subject))];

    container.innerHTML = `
      <div class="page-container">
        <div class="history-header">
          <h1>ðŸ•’ History</h1>
        </div>

        <!-- Tabs -->
        <div class="history-tabs">
          <div class="tab-bar">
            <div class="tab-item ${activeTab === 'quiz' ? 'active' : ''}" data-tab="quiz">ðŸ“ Quiz History</div>
            <div class="tab-item ${activeTab === 'exam' ? 'active' : ''}" data-tab="exam">ðŸŽ¯ Exam History</div>
          </div>
        </div>

        <!-- Filters -->
        <div class="history-filters">
          <div class="history-search">
            <span class="history-search-icon">ðŸ”</span>
            <input type="text" class="input-field" id="history-search" placeholder="Search by topic or subject..." value="${searchQuery}">
          </div>
          <select class="select-field history-filter-select" id="history-filter-subject">
            <option value="">All Subjects</option>
            ${allSubjects.map(s => `<option value="${s}" ${filterSubject === s ? 'selected' : ''}>${s}</option>`).join('')}
          </select>
        </div>

        <!-- List -->
        <div class="history-list" id="history-list">
          ${filtered.length === 0 ? `
            <div class="empty-state">
              <div class="empty-state-icon">${activeTab === 'quiz' ? 'ðŸ“' : 'ðŸŽ¯'}</div>
              <h2 class="empty-state-title">No ${activeTab === 'quiz' ? 'quizzes' : 'exams'} yet</h2>
              <p class="empty-state-text">${searchQuery || filterSubject ? 'No results match your filters. Try adjusting your search.' : `Start a ${activeTab === 'quiz' ? 'quiz' : 'practice exam'} to see your history here!`}</p>
              ${!searchQuery && !filterSubject ? `
                <button class="btn btn-primary" style="margin-top: var(--space-5);" id="history-start-btn">
                  ${activeTab === 'quiz' ? 'ðŸ“ Generate Quiz' : 'ðŸŽ¯ Start Practice Exam'}
                </button>
              ` : ''}
            </div>
          ` : filtered.map((item, index) => `
            <div class="history-card animate-item" data-id="${item.id}" style="animation-delay: ${index * 40}ms;">
              <div class="history-card-icon ${item.type}">
                ${item.type === 'quiz' ? 'ðŸ“' : 'ðŸŽ¯'}
              </div>
              <div class="history-card-info">
                <div class="history-card-title">${item.topic}</div>
                <div class="history-card-meta">
                  <span>ðŸ“… ${formatDate(item.date)}</span>
                  <span>â± ${item.duration}</span>
                  <span>ðŸ“Š ${item.correct}/${item.total} correct</span>
                </div>
              </div>
              <div class="history-card-score ${item.score >= 80 ? 'high' : item.score >= 60 ? 'medium' : 'low'}">
                ${item.score}%
              </div>
              <div class="history-card-actions">
                <button class="btn btn-ghost btn-icon retake-btn" data-id="${item.id}" title="Retake">ðŸ”„</button>
              </div>
            </div>
          `).join('')}
        </div>
      </div>
    `;

    staggerReveal(container, '.animate-item', 40);

    // Tab switching
    container.querySelectorAll('.tab-item').forEach(tab => {
      tab.addEventListener('click', () => {
        activeTab = tab.dataset.tab;
        render();
      });
    });

    // Search
    const searchInput = container.querySelector('#history-search');
    let searchTimeout;
    searchInput.addEventListener('input', (e) => {
      clearTimeout(searchTimeout);
      searchTimeout = setTimeout(() => {
        searchQuery = e.target.value;
        render();
      }, 300);
    });

    // Filter
    container.querySelector('#history-filter-subject').addEventListener('change', (e) => {
      filterSubject = e.target.value;
      render();
    });

    // Retake buttons
    container.querySelectorAll('.retake-btn').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        router.navigate(activeTab === 'quiz' ? 'quiz' : 'exam');
      });
    });

    // Start button in empty state
    const startBtn = container.querySelector('#history-start-btn');
    if (startBtn) {
      startBtn.addEventListener('click', () => {
        router.navigate(activeTab === 'quiz' ? 'quiz' : 'exam');
      });
    }

    // Card click â†’ could open detail
    container.querySelectorAll('.history-card').forEach(card => {
      card.addEventListener('click', () => {
        // For now, just highlight
        card.style.borderColor = 'var(--color-primary)';
        setTimeout(() => {
          card.style.borderColor = '';
        }, 800);
      });
    });
  }

  render();
}

function formatDate(dateStr) {
  const date = new Date(dateStr);
  const now = new Date();
  const diff = Math.floor((now - date) / (1000 * 60 * 60 * 24));

  if (diff === 0) return 'Today';
  if (diff === 1) return 'Yesterday';
  if (diff < 7) return `${diff} days ago`;

  return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
}


=== js/components/progress.js ===

// ============================================
// DHRYZN â€” Progress Component
// ============================================

import { drawBarChart, drawLineChart, drawCircularProgress } from '../utils/charts.js';
import { staggerReveal, countUpAnimation } from '../utils/animations.js';
import { weekDays } from '../data/sampleData.js';

export function renderProgress(container, params, store, router) {
  const progress = store.get('progress');

  container.innerHTML = `
    <div class="page-container">
      <div class="progress-page-header">
        <h1>ðŸ“ˆ Your Progress</h1>
        <p>Track your learning journey and identify areas for improvement.</p>
      </div>

      <!-- Stats Grid -->
      <div class="progress-stats-grid">
        <div class="progress-stat-card animate-item">
          <div class="progress-stat-icon">ðŸ”¥</div>
          <div class="progress-stat-value" data-count="${progress.streak}">${progress.streak}</div>
          <div class="progress-stat-label">Day Streak</div>
        </div>
        <div class="progress-stat-card animate-item">
          <div class="progress-stat-icon">ðŸ“</div>
          <div class="progress-stat-value" data-count="${progress.totalQuestions}">${progress.totalQuestions}</div>
          <div class="progress-stat-label">Questions Answered</div>
        </div>
        <div class="progress-stat-card animate-item">
          <div class="progress-stat-icon">ðŸŽ¯</div>
          <div class="progress-stat-value" data-count="${progress.averageAccuracy}" data-suffix="%">${progress.averageAccuracy}%</div>
          <div class="progress-stat-label">Average Accuracy</div>
        </div>
        <div class="progress-stat-card animate-item">
          <div class="progress-stat-icon">â±</div>
          <div class="progress-stat-value">${formatStudyTime(progress.studyTimeMinutes)}</div>
          <div class="progress-stat-label">Total Study Time</div>
        </div>
      </div>

      <!-- Charts -->
      <div class="progress-charts">
        <!-- Mastery Level -->
        <div class="progress-chart-card animate-item">
          <div class="progress-chart-header">
            <div>
              <div class="progress-chart-title">Mastery Level</div>
              <div class="progress-chart-subtitle">Overall subject mastery</div>
            </div>
          </div>
          <div class="mastery-section">
            <div class="mastery-canvas-wrap">
              <canvas id="mastery-chart"></canvas>
              <div class="mastery-value">
                <span class="mastery-percent">${progress.mastery}%</span>
                <span class="mastery-label">Mastery</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Exam Readiness -->
        <div class="progress-chart-card animate-item">
          <div class="progress-chart-header">
            <div>
              <div class="progress-chart-title">Exam Readiness</div>
              <div class="progress-chart-subtitle">How prepared you are</div>
            </div>
          </div>
          <div class="readiness-section">
            <div class="readiness-chart-wrap">
              <canvas id="readiness-chart"></canvas>
              <div class="readiness-chart-value">${progress.examReadiness}%</div>
            </div>
            <div class="readiness-details">
              <div class="readiness-status">${getReadinessStatus(progress.examReadiness)}</div>
              <div class="readiness-desc">${getReadinessDesc(progress.examReadiness)}</div>
            </div>
          </div>
        </div>

        <!-- Weekly Activity -->
        <div class="progress-chart-card full-width animate-item">
          <div class="progress-chart-header">
            <div>
              <div class="progress-chart-title">Weekly Learning Activity</div>
              <div class="progress-chart-subtitle">Questions answered per day</div>
            </div>
          </div>
          <div class="chart-container">
            <canvas id="weekly-chart"></canvas>
          </div>
        </div>

        <!-- Strong Topics -->
        <div class="progress-chart-card animate-item">
          <div class="progress-chart-header">
            <div>
              <div class="progress-chart-title">Strong Topics</div>
              <div class="progress-chart-subtitle">Areas where you excel</div>
            </div>
          </div>
          <div class="topics-list">
            ${progress.strongTopics.map(t => `
              <div class="topic-item">
                <div class="topic-item-header">
                  <span class="topic-name">${t.name}</span>
                  <span class="topic-percent">${t.percent}%</span>
                </div>
                <div class="topic-bar">
                  <div class="topic-bar-fill strong" style="width: ${t.percent}%;"></div>
                </div>
              </div>
            `).join('')}
          </div>
        </div>

        <!-- Weak Topics -->
        <div class="progress-chart-card animate-item">
          <div class="progress-chart-header">
            <div>
              <div class="progress-chart-title">Weak Topics</div>
              <div class="progress-chart-subtitle">Areas to focus on</div>
            </div>
          </div>
          <div class="topics-list">
            ${progress.weakTopics.map(t => `
              <div class="topic-item">
                <div class="topic-item-header">
                  <span class="topic-name">${t.name}</span>
                  <span class="topic-percent">${t.percent}%</span>
                </div>
                <div class="topic-bar">
                  <div class="topic-bar-fill ${t.percent >= 60 ? 'medium' : 'weak'}" style="width: ${t.percent}%;"></div>
                </div>
              </div>
            `).join('')}
          </div>
        </div>

        <!-- Exam Trend -->
        <div class="progress-chart-card full-width animate-item">
          <div class="progress-chart-header">
            <div>
              <div class="progress-chart-title">Practice Exam Trend</div>
              <div class="progress-chart-subtitle">Your score progression over recent exams</div>
            </div>
          </div>
          <div class="chart-container">
            <canvas id="trend-chart"></canvas>
          </div>
        </div>
      </div>

      <!-- Recommended Action -->
      <div class="recommended-action animate-item" id="recommended-action">
        <div class="recommended-icon">ðŸ’¡</div>
        <div class="recommended-content">
          <div class="recommended-title">${getRecommendation(progress).title}</div>
          <div class="recommended-desc">${getRecommendation(progress).desc}</div>
        </div>
        <div class="recommended-arrow">â†’</div>
      </div>
    </div>
  `;

  staggerReveal(container, '.animate-item', 70);

  // Draw charts after render
  requestAnimationFrame(() => {
    // Mastery
    const masteryCanvas = container.querySelector('#mastery-chart');
    if (masteryCanvas) {
      drawCircularProgress(masteryCanvas, progress.mastery, { size: 160, lineWidth: 12 });
    }

    // Readiness
    const readinessCanvas = container.querySelector('#readiness-chart');
    if (readinessCanvas) {
      drawCircularProgress(readinessCanvas, progress.examReadiness, { size: 120, lineWidth: 10 });
    }

    // Weekly activity bar chart
    const weeklyCanvas = container.querySelector('#weekly-chart');
    if (weeklyCanvas) {
      const parentWidth = weeklyCanvas.parentElement.offsetWidth;
      drawBarChart(weeklyCanvas, progress.weeklyActivity, weekDays, {
        width: Math.min(parentWidth, 700),
        height: 200
      });
    }

    // Exam trend line chart
    const trendCanvas = container.querySelector('#trend-chart');
    if (trendCanvas) {
      const parentWidth = trendCanvas.parentElement.offsetWidth;
      const labels = progress.examTrend.map((_, i) => `Exam ${i + 1}`);
      drawLineChart(trendCanvas, progress.examTrend, labels, {
        width: Math.min(parentWidth, 700),
        height: 200
      });
    }
  });

  // Count-up animations
  container.querySelectorAll('[data-count]').forEach(el => {
    const target = parseInt(el.dataset.count);
    const suffix = el.dataset.suffix || '';
    countUpAnimation(el, target, 1200, suffix);
  });

  // Recommended action click
  container.querySelector('#recommended-action')?.addEventListener('click', () => {
    const rec = getRecommendation(progress);
    router.navigate(rec.route);
  });
}

function formatStudyTime(minutes) {
  if (minutes < 60) return `${minutes}m`;
  const hours = Math.floor(minutes / 60);
  const mins = minutes % 60;
  return `${hours}h ${mins}m`;
}

function getReadinessStatus(percent) {
  if (percent >= 85) return 'ðŸŸ¢ Exam Ready';
  if (percent >= 70) return 'ðŸŸ¡ Almost There';
  if (percent >= 50) return 'ðŸŸ  Getting Closer';
  return 'ðŸ”´ Keep Practicing';
}

function getReadinessDesc(percent) {
  if (percent >= 85) return 'You\'re well prepared! Consider taking a practice exam to confirm.';
  if (percent >= 70) return 'A few more study sessions and you\'ll be ready. Focus on weak areas.';
  if (percent >= 50) return 'You\'re making progress! Keep studying consistently.';
  return 'Don\'t worry â€” consistent practice will get you there. Start with the basics.';
}

function getRecommendation(progress) {
  if (progress.weakTopics.length > 0) {
    const weakest = progress.weakTopics[0];
    return {
      title: `Practice ${weakest.name}`,
      desc: `Your accuracy on ${weakest.name} is ${weakest.percent}%. A focused quiz session could help improve this area significantly.`,
      route: 'quiz'
    };
  }
  return {
    title: 'Take a Practice Exam',
    desc: 'You\'re doing great! Try a practice exam to test your overall readiness.',
    route: 'exam'
  };
}


=== js/components/quiz.js ===

// ============================================
// DHRYZN â€” Quiz Component
// ============================================

import { generateQuizQuestions } from '../utils/mockAI.js';
import { staggerReveal } from '../utils/animations.js';
import { drawCircularProgress } from '../utils/charts.js';
import { difficultyLevels, gradeLevels, questionTypes } from '../data/sampleData.js';

export function renderQuiz(container, params, store, router) {
  const subjectId = params[0];
  const subjects = store.get('subjects');
  const subject = subjectId ? subjects.find(s => s.id === subjectId) : null;

  renderQuizConfig(container, subject, store, router);
}

function renderQuizConfig(container, subject, store, router) {
  container.innerHTML = `
    <div class="page-container">
      <div class="quiz-config">
        <div class="quiz-config-header">
          <h1>ðŸ“ Generate Quiz</h1>
          <p>Create a personalized quiz to test your knowledge and reinforce learning.</p>
        </div>

        <div class="quiz-config-form">
          <div class="form-group animate-item">
            <label>Topic</label>
            <input type="text" class="input-field" id="quiz-topic" placeholder="e.g., Quadratic Equations" value="${subject ? subject.name : ''}">
          </div>

          <div class="form-row animate-item">
            <div class="form-group">
              <label>Grade Level</label>
              <select class="select-field" id="quiz-grade">
                ${gradeLevels.map(g => `<option value="${g}">${g}</option>`).join('')}
              </select>
            </div>
            <div class="form-group">
              <label>Difficulty</label>
              <select class="select-field" id="quiz-difficulty">
                ${difficultyLevels.map(d => `<option value="${d}" ${d === 'Medium' ? 'selected' : ''}>${d}</option>`).join('')}
              </select>
            </div>
          </div>

          <div class="form-group animate-item">
            <label>Number of Questions</label>
            <select class="select-field" id="quiz-count">
              <option value="5">5 Questions</option>
              <option value="10" selected>10 Questions</option>
              <option value="15">15 Questions</option>
              <option value="20">20 Questions</option>
            </select>
          </div>

          <div class="form-group animate-item">
            <label>Question Type</label>
            <div class="question-type-grid" id="question-type-grid">
              ${questionTypes.map((t, i) => `
                <div class="question-type-option${i === 0 ? ' selected' : ''}" data-type="${t}">${t}</div>
              `).join('')}
            </div>
          </div>

          <div class="quiz-config-actions animate-item">
            <button class="btn btn-secondary" id="quiz-back-btn">Cancel</button>
            <button class="btn btn-primary btn-lg" id="quiz-start-btn">ðŸš€ Start Quiz</button>
          </div>
        </div>
      </div>
    </div>
  `;

  staggerReveal(container, '.animate-item', 60);

  // Question type toggle
  let selectedType = 'Multiple Choice';
  container.querySelector('#question-type-grid').addEventListener('click', (e) => {
    const opt = e.target.closest('.question-type-option');
    if (!opt) return;
    container.querySelectorAll('.question-type-option').forEach(o => o.classList.remove('selected'));
    opt.classList.add('selected');
    selectedType = opt.dataset.type;
  });

  // Back
  container.querySelector('#quiz-back-btn').addEventListener('click', () => {
    window.history.back();
  });

  // Start quiz
  container.querySelector('#quiz-start-btn').addEventListener('click', () => {
    const topic = container.querySelector('#quiz-topic').value.trim() || 'General Knowledge';
    const count = parseInt(container.querySelector('#quiz-count').value);
    const difficulty = container.querySelector('#quiz-difficulty').value;

    const questions = generateQuizQuestions(topic, count, selectedType, difficulty);
    renderQuizTaking(container, questions, topic, selectedType, store, router);
  });
}

function renderQuizTaking(container, questions, topic, questionType, store, router) {
  let currentQuestion = 0;
  let answers = new Array(questions.length).fill(null);
  let answered = new Array(questions.length).fill(false);
  let startTime = Date.now();

  function render() {
    const q = questions[currentQuestion];
    const isAnswered = answered[currentQuestion];
    const isTF = questionType === 'True / False';
    const isText = questionType === 'Short Answer' || questionType === 'Essay';
    const progress = ((currentQuestion + 1) / questions.length) * 100;

    container.innerHTML = `
      <div class="page-container">
        <div class="quiz-taking">
          <div class="quiz-progress-header">
            <span class="quiz-progress-text">Question ${currentQuestion + 1} of ${questions.length}</span>
            <span class="quiz-progress-text">${topic}</span>
          </div>
          <div class="quiz-progress-bar">
            <div class="progress-bar">
              <div class="progress-bar-fill" style="width: ${progress}%"></div>
            </div>
          </div>

          <div class="quiz-question-card">
            <div class="quiz-question-number">Question ${currentQuestion + 1}</div>
            <div class="quiz-question-text">${q.question}</div>

            ${isText ? `
              <textarea class="quiz-text-input" id="text-answer" placeholder="Type your answer here..." rows="${questionType === 'Essay' ? 6 : 2}">${answers[currentQuestion] || ''}</textarea>
            ` : isTF ? `
              <div class="quiz-options">
                <div class="quiz-option ${answers[currentQuestion] === true ? 'selected' : ''} ${isAnswered ? (q.answer === true ? 'correct' : answers[currentQuestion] === true ? 'incorrect' : '') : ''} ${isAnswered ? 'disabled' : ''}" data-answer="true">
                  <div class="quiz-option-letter">${answers[currentQuestion] === true && isAnswered ? (q.answer === true ? 'âœ“' : 'âœ—') : 'T'}</div>
                  <span>True</span>
                </div>
                <div class="quiz-option ${answers[currentQuestion] === false ? 'selected' : ''} ${isAnswered ? (q.answer === false ? 'correct' : answers[currentQuestion] === false ? 'incorrect' : '') : ''} ${isAnswered ? 'disabled' : ''}" data-answer="false">
                  <div class="quiz-option-letter">${answers[currentQuestion] === false && isAnswered ? (q.answer === false ? 'âœ“' : 'âœ—') : 'F'}</div>
                  <span>False</span>
                </div>
              </div>
            ` : `
              <div class="quiz-options">
                ${q.options.map((opt, i) => {
                  const letter = String.fromCharCode(65 + i);
                  const isSelected = answers[currentQuestion] === i;
                  const isCorrect = q.correct === i;
                  let classes = 'quiz-option';
                  if (isSelected) classes += ' selected';
                  if (isAnswered) {
                    classes += ' disabled';
                    if (isCorrect) classes += ' correct';
                    else if (isSelected && !isCorrect) classes += ' incorrect';
                  }
                  let letterContent = letter;
                  if (isAnswered && isCorrect) letterContent = 'âœ“';
                  else if (isAnswered && isSelected && !isCorrect) letterContent = 'âœ—';
                  return `
                    <div class="${classes}" data-index="${i}">
                      <div class="quiz-option-letter">${letterContent}</div>
                      <span>${opt}</span>
                    </div>
                  `;
                }).join('')}
              </div>
            `}

            ${isAnswered && q.explanation ? `
              <div class="quiz-feedback ${isTF ? (answers[currentQuestion] === q.answer ? 'correct' : 'incorrect') : (answers[currentQuestion] === q.correct ? 'correct' : 'incorrect')}">
                <div class="quiz-feedback-title">
                  ${isTF ? (answers[currentQuestion] === q.answer ? 'âœ… Correct!' : 'ðŸ’¡ Nice try!') : (answers[currentQuestion] === q.correct ? 'âœ… Correct!' : 'ðŸ’¡ Nice try!')}
                </div>
                <div>${q.explanation}</div>
              </div>
            ` : ''}
          </div>

          <div class="quiz-nav-actions">
            <button class="btn btn-secondary" id="quiz-prev" ${currentQuestion === 0 ? 'disabled style="opacity:0.4"' : ''}>â† Previous</button>
            <div style="display: flex; gap: var(--space-3);">
              ${!isAnswered && !isText ? '<button class="btn btn-secondary" id="quiz-check" disabled>Check Answer</button>' : ''}
              ${isText && !isAnswered ? '<button class="btn btn-primary" id="quiz-submit-text">Submit Answer</button>' : ''}
              ${currentQuestion < questions.length - 1 ? `
                <button class="btn btn-primary" id="quiz-next">Next â†’</button>
              ` : `
                <button class="btn btn-primary" id="quiz-finish">ðŸŽ‰ Finish Quiz</button>
              `}
            </div>
          </div>
        </div>
      </div>
    `;

    // Event handlers
    if (!isText && !isAnswered) {
      container.querySelectorAll('.quiz-option').forEach(opt => {
        opt.addEventListener('click', () => {
          if (isAnswered) return;

          if (isTF) {
            answers[currentQuestion] = opt.dataset.answer === 'true';
          } else {
            answers[currentQuestion] = parseInt(opt.dataset.index);
          }

          container.querySelectorAll('.quiz-option').forEach(o => o.classList.remove('selected'));
          opt.classList.add('selected');

          const checkBtn = container.querySelector('#quiz-check');
          if (checkBtn) checkBtn.disabled = false;
        });
      });

      const checkBtn = container.querySelector('#quiz-check');
      if (checkBtn) {
        checkBtn.addEventListener('click', () => {
          answered[currentQuestion] = true;
          render();
        });
      }
    }

    // Text answer submit
    const submitTextBtn = container.querySelector('#quiz-submit-text');
    if (submitTextBtn) {
      submitTextBtn.addEventListener('click', () => {
        const textEl = container.querySelector('#text-answer');
        answers[currentQuestion] = textEl.value.trim();
        answered[currentQuestion] = true;
        render();
      });
    }

    // Navigation
    const prevBtn = container.querySelector('#quiz-prev');
    if (prevBtn && currentQuestion > 0) {
      prevBtn.addEventListener('click', () => { currentQuestion--; render(); });
    }

    const nextBtn = container.querySelector('#quiz-next');
    if (nextBtn) {
      nextBtn.addEventListener('click', () => { currentQuestion++; render(); });
    }

    const finishBtn = container.querySelector('#quiz-finish');
    if (finishBtn) {
      finishBtn.addEventListener('click', () => {
        const elapsed = Date.now() - startTime;
        renderQuizResults(container, questions, answers, answered, questionType, topic, elapsed, store, router);
      });
    }
  }

  render();
}

function renderQuizResults(container, questions, answers, answered, questionType, topic, elapsed, store, router) {
  const isTF = questionType === 'True / False';
  const isText = questionType === 'Short Answer' || questionType === 'Essay';

  let correct = 0;
  if (isText) {
    correct = answers.filter(a => a && a.length > 0).length; // All text answers count as correct for demo
  } else if (isTF) {
    questions.forEach((q, i) => { if (answers[i] === q.answer) correct++; });
  } else {
    questions.forEach((q, i) => { if (answers[i] === q.correct) correct++; });
  }

  const score = Math.round((correct / questions.length) * 100);
  const minutes = Math.floor(elapsed / 60000);
  const seconds = Math.floor((elapsed % 60000) / 1000);
  const duration = `${minutes}m ${seconds}s`;

  // Save to history
  store.addQuizToHistory({
    id: 'q-' + Date.now(),
    subject: topic,
    topic: topic,
    date: new Date().toISOString().split('T')[0],
    duration: `${minutes} min`,
    score,
    total: questions.length,
    correct,
    type: 'quiz',
    status: 'completed'
  });

  container.innerHTML = `
    <div class="page-container">
      <div class="quiz-results">
        <h1 style="font-size: var(--text-h1); margin-bottom: var(--space-2);">
          ${score >= 80 ? 'ðŸŽ‰ Excellent!' : score >= 60 ? 'ðŸ‘ Good Job!' : 'ðŸ’ª Keep Practicing!'}
        </h1>
        <p class="text-secondary">${score >= 80 ? 'You really know your stuff!' : score >= 60 ? 'You\'re on the right track!' : 'Every attempt makes you stronger!'}</p>

        <div class="quiz-results-score">
          <div class="quiz-score-circle">
            <canvas id="score-chart"></canvas>
            <div class="quiz-score-value">${score}%</div>
          </div>
        </div>

        <div class="quiz-results-stats">
          <div class="quiz-result-stat">
            <div class="quiz-result-stat-value" style="color: var(--color-success);">${correct}</div>
            <div class="quiz-result-stat-label">Correct</div>
          </div>
          <div class="quiz-result-stat">
            <div class="quiz-result-stat-value" style="color: var(--color-error);">${questions.length - correct}</div>
            <div class="quiz-result-stat-label">Incorrect</div>
          </div>
          <div class="quiz-result-stat">
            <div class="quiz-result-stat-value">${duration}</div>
            <div class="quiz-result-stat-label">Time Taken</div>
          </div>
        </div>

        <div class="quiz-results-actions">
          <button class="btn btn-secondary" id="results-review">ðŸ“‹ Review Answers</button>
          <button class="btn btn-primary" id="results-retry">ðŸ”„ Try Again</button>
          <button class="btn btn-secondary" id="results-home">ðŸ  Dashboard</button>
        </div>

        <div class="quiz-review-list" id="review-list" style="display: none;">
          <h3>Answer Review</h3>
          ${questions.map((q, i) => {
            let isCorrectAnswer;
            if (isTF) isCorrectAnswer = answers[i] === q.answer;
            else if (isText) isCorrectAnswer = answers[i] && answers[i].length > 0;
            else isCorrectAnswer = answers[i] === q.correct;

            return `
              <div class="quiz-review-item ${isCorrectAnswer ? 'correct' : 'incorrect'}">
                <div class="quiz-review-question">${i + 1}. ${q.question}</div>
                <div class="quiz-review-answer">
                  ${isText ? `Your answer: ${answers[i] || 'Not answered'}` :
                    isTF ? `Your answer: ${answers[i] === null ? 'Not answered' : answers[i] ? 'True' : 'False'} â€¢ Correct: ${q.answer ? 'True' : 'False'}` :
                    `Your answer: ${answers[i] !== null ? q.options[answers[i]] : 'Not answered'} â€¢ Correct: ${q.options[q.correct]}`}
                </div>
              </div>
            `;
          }).join('')}
        </div>
      </div>
    </div>
  `;

  // Draw score chart
  const canvas = container.querySelector('#score-chart');
  if (canvas) {
    drawCircularProgress(canvas, score, { size: 140, lineWidth: 10 });
  }

  // Event handlers
  container.querySelector('#results-review').addEventListener('click', () => {
    const list = container.querySelector('#review-list');
    list.style.display = list.style.display === 'none' ? 'block' : 'none';
  });

  container.querySelector('#results-retry').addEventListener('click', () => {
    router.navigate('quiz');
  });

  container.querySelector('#results-home').addEventListener('click', () => {
    router.navigate('dashboard');
  });
}


=== js/components/settings.js ===

// ============================================
// DHRYZN â€” Settings Component
// ============================================

import { staggerReveal } from '../utils/animations.js';
import { accentColors, difficultyLevels, questionTypes, explanationStyles } from '../data/sampleData.js';

export function renderSettings(container, params, store, router) {
  const settings = store.get('settings');

  container.innerHTML = `
    <div class="page-container">
      <div class="settings-header">
        <h1>âš™ï¸ Settings</h1>
        <p>Customize your DHRYZN experience.</p>
      </div>

      <div class="settings-sections">
        <!-- Appearance -->
        <div class="settings-section animate-item">
          <div class="settings-section-title"><span>ðŸŽ¨</span> Appearance</div>
          <div class="settings-group">
            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Dark Theme</div>
                <div class="settings-item-desc">Use a dark color scheme for the interface</div>
              </div>
              <div class="settings-item-control">
                <label class="toggle-switch">
                  <input type="checkbox" id="setting-theme" ${settings.theme === 'dark' ? 'checked' : ''}>
                  <div class="toggle-track"></div>
                </label>
              </div>
            </div>

            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Accent Color</div>
                <div class="settings-item-desc">Choose your preferred accent color</div>
              </div>
              <div class="settings-item-control">
                <div class="color-options">
                  ${accentColors.map(c => `
                    <div class="color-option ${settings.accentColor === c.value ? 'active' : ''}" 
                         data-color="${c.value}" 
                         style="background: ${c.value}; color: ${c.value};" 
                         title="${c.name}">
                    </div>
                  `).join('')}
                </div>
              </div>
            </div>

            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Language</div>
                <div class="settings-item-desc">Display language for the interface</div>
              </div>
              <div class="settings-item-control">
                <select class="select-field settings-select" id="setting-language">
                  <option value="English" ${settings.language === 'English' ? 'selected' : ''}>English</option>
                  <option value="Spanish" ${settings.language === 'Spanish' ? 'selected' : ''}>EspaÃ±ol</option>
                  <option value="French" ${settings.language === 'French' ? 'selected' : ''}>FranÃ§ais</option>
                  <option value="Indonesian" ${settings.language === 'Indonesian' ? 'selected' : ''}>Bahasa Indonesia</option>
                </select>
              </div>
            </div>
          </div>
        </div>

        <!-- Learning Preferences -->
        <div class="settings-section animate-item">
          <div class="settings-section-title"><span>ðŸ“–</span> Learning Preferences</div>
          <div class="settings-group">
            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Default Difficulty</div>
                <div class="settings-item-desc">Default difficulty for quizzes and exams</div>
              </div>
              <div class="settings-item-control">
                <select class="select-field settings-select" id="setting-difficulty">
                  ${difficultyLevels.map(d => `
                    <option value="${d}" ${settings.defaultDifficulty === d ? 'selected' : ''}>${d}</option>
                  `).join('')}
                </select>
              </div>
            </div>

            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Preferred Question Type</div>
                <div class="settings-item-desc">Default type for generated quizzes</div>
              </div>
              <div class="settings-item-control">
                <select class="select-field settings-select" id="setting-question-type">
                  ${questionTypes.map(t => `
                    <option value="${t}" ${settings.preferredQuestionType === t ? 'selected' : ''}>${t}</option>
                  `).join('')}
                </select>
              </div>
            </div>

            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Explanation Style</div>
                <div class="settings-item-desc">How topics are explained by default</div>
              </div>
              <div class="settings-item-control">
                <select class="select-field settings-select" id="setting-explanation-style">
                  ${explanationStyles.map(s => `
                    <option value="${s}" ${settings.explanationStyle === s ? 'selected' : ''}>${s}</option>
                  `).join('')}
                </select>
              </div>
            </div>

            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Daily Goal</div>
                <div class="settings-item-desc">Target study time per day (minutes): <strong id="goal-display">${settings.dailyGoal} min</strong></div>
              </div>
              <div class="settings-item-control">
                <input type="range" class="settings-slider" id="setting-daily-goal" min="10" max="120" step="5" value="${settings.dailyGoal}">
              </div>
            </div>

            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Timer in Practice Exams</div>
                <div class="settings-item-desc">Show countdown timer during practice exams</div>
              </div>
              <div class="settings-item-control">
                <label class="toggle-switch">
                  <input type="checkbox" id="setting-timer" ${settings.timerEnabled ? 'checked' : ''}>
                  <div class="toggle-track"></div>
                </label>
              </div>
            </div>
          </div>
        </div>

        <!-- Reset -->
        <div class="settings-section animate-item">
          <div class="settings-section-title"><span>ðŸ”„</span> Data</div>
          <div class="settings-group">
            <div class="settings-item">
              <div class="settings-item-info">
                <div class="settings-item-label">Reset to Default</div>
                <div class="settings-item-desc">Clear all data and return settings to their default values</div>
              </div>
              <div class="settings-item-control">
                <button class="btn btn-secondary settings-reset-btn" id="reset-btn" style="color: var(--color-error); border-color: rgba(248,113,113,0.3);">Reset All</button>
              </div>
            </div>
          </div>
        </div>

        <!-- About -->
        <div class="settings-section animate-item">
          <div class="settings-section-title"><span>â„¹ï¸</span> About</div>
          <div class="settings-about">
            <div class="settings-about-logo">DHRYZN</div>
            <div class="settings-about-version">Version 1.0.0</div>
            <div class="settings-about-desc">
              DHRYZN is your personal AI Study Mentor. Designed to help students learn, practice, and excel through guided learning, quizzes, and exam simulations.
            </div>
          </div>
        </div>
      </div>

      <!-- Reset Confirmation Modal -->
      <div class="modal-overlay" id="reset-modal" style="display: none;">
        <div class="modal">
          <div class="modal-header">
            <h2 class="modal-title">âš ï¸ Reset All Data?</h2>
            <button class="modal-close" id="reset-modal-close">âœ•</button>
          </div>
          <p style="color: var(--color-text-secondary); margin-bottom: var(--space-6); line-height: var(--leading-relaxed);">
            This will clear all your quiz history, exam history, progress data, and settings. This action cannot be undone.
          </p>
          <div style="display: flex; gap: var(--space-3); justify-content: flex-end;">
            <button class="btn btn-secondary" id="reset-cancel">Cancel</button>
            <button class="btn btn-primary" id="reset-confirm" style="background: var(--color-error); box-shadow: none;">Reset Everything</button>
          </div>
        </div>
      </div>

      <!-- Toast Container -->
      <div class="toast-container" id="toast-container"></div>
    </div>
  `;

  staggerReveal(container, '.animate-item', 60);

  // --- Event Handlers ---

  // Theme toggle
  container.querySelector('#setting-theme').addEventListener('change', (e) => {
    updateSetting('theme', e.target.checked ? 'dark' : 'light');
    showToast('Theme updated');
  });

  // Accent color
  container.querySelectorAll('.color-option').forEach(opt => {
    opt.addEventListener('click', () => {
      container.querySelectorAll('.color-option').forEach(o => o.classList.remove('active'));
      opt.classList.add('active');
      const color = opt.dataset.color;
      updateSetting('accentColor', color);
      
      // Apply accent color to CSS variable
      document.documentElement.style.setProperty('--color-primary', color);
      showToast('Accent color updated');
    });
  });

  // Language
  container.querySelector('#setting-language').addEventListener('change', (e) => {
    updateSetting('language', e.target.value);
    showToast('Language preference saved');
  });

  // Difficulty
  container.querySelector('#setting-difficulty').addEventListener('change', (e) => {
    updateSetting('defaultDifficulty', e.target.value);
  });

  // Question type
  container.querySelector('#setting-question-type').addEventListener('change', (e) => {
    updateSetting('preferredQuestionType', e.target.value);
  });

  // Explanation style
  container.querySelector('#setting-explanation-style').addEventListener('change', (e) => {
    updateSetting('explanationStyle', e.target.value);
  });

  // Daily goal slider
  const goalSlider = container.querySelector('#setting-daily-goal');
  const goalDisplay = container.querySelector('#goal-display');
  goalSlider.addEventListener('input', (e) => {
    goalDisplay.textContent = `${e.target.value} min`;
  });
  goalSlider.addEventListener('change', (e) => {
    updateSetting('dailyGoal', parseInt(e.target.value));
    showToast('Daily goal updated');
  });

  // Timer toggle
  container.querySelector('#setting-timer').addEventListener('change', (e) => {
    updateSetting('timerEnabled', e.target.checked);
  });

  // Reset modal
  const resetModal = container.querySelector('#reset-modal');
  container.querySelector('#reset-btn').addEventListener('click', () => {
    resetModal.style.display = 'flex';
  });
  container.querySelector('#reset-modal-close').addEventListener('click', () => {
    resetModal.style.display = 'none';
  });
  container.querySelector('#reset-cancel').addEventListener('click', () => {
    resetModal.style.display = 'none';
  });
  resetModal.addEventListener('click', (e) => {
    if (e.target === resetModal) resetModal.style.display = 'none';
  });
  container.querySelector('#reset-confirm').addEventListener('click', () => {
    store.reset();
    resetModal.style.display = 'none';
    showToast('All data has been reset');
    // Re-render after reset
    setTimeout(() => renderSettings(container, params, store, router), 500);
  });

  function updateSetting(key, value) {
    store.update('settings', s => ({ ...s, [key]: value }));
  }

  function showToast(message) {
    const toastContainer = container.querySelector('#toast-container');
    if (!toastContainer) return;
    
    const toast = document.createElement('div');
    toast.className = 'toast toast-success';
    toast.innerHTML = `<span>âœ…</span> <span>${message}</span>`;
    toastContainer.appendChild(toast);

    setTimeout(() => {
      toast.style.animation = 'fadeOut 0.3s ease forwards';
      setTimeout(() => toast.remove(), 300);
    }, 2500);
  }
}


=== js/components/sidebar.js ===

// ============================================
// DHRYZN â€” Sidebar Component
// ============================================

export function renderSidebar(container, router) {
  const navItems = [
    { id: 'dashboard', icon: 'ðŸ ', label: 'Dashboard', route: 'dashboard' },
    { id: 'subjects', icon: 'ðŸ“š', label: 'Subjects', route: 'subjects' },
    { id: 'progress', icon: 'ðŸ“ˆ', label: 'Progress', route: 'progress' },
    { id: 'history', icon: 'ðŸ•’', label: 'History', route: 'history' },
    { id: 'settings', icon: 'âš™ï¸', label: 'Settings', route: 'settings' }
  ];

  const sidebar = document.createElement('aside');
  sidebar.className = 'sidebar';
  sidebar.id = 'sidebar';
  sidebar.innerHTML = `
    <div class="sidebar-brand">
      <div class="sidebar-logo">D</div>
      <span class="sidebar-brand-text">DHRYZN</span>
    </div>
    <nav class="sidebar-nav">
      ${navItems.map(item => `
        <div class="sidebar-nav-item${item.route === 'dashboard' ? ' active' : ''}" data-route="${item.route}" id="nav-${item.id}">
          <span class="sidebar-nav-icon">${item.icon}</span>
          <span class="sidebar-nav-label">${item.label}</span>
        </div>
      `).join('')}
    </nav>
    <div class="sidebar-footer">
      <p class="sidebar-footer-text">DHRYZN v1.0 â€” AI Study Mentor</p>
    </div>
  `;

  // Nav click handlers
  sidebar.querySelectorAll('.sidebar-nav-item').forEach(item => {
    item.addEventListener('click', () => {
      const route = item.dataset.route;
      router.navigate(route);

      // Close mobile menu
      sidebar.classList.remove('open');
      const overlay = document.getElementById('sidebar-overlay');
      if (overlay) overlay.classList.remove('visible');
    });
  });

  container.appendChild(sidebar);

  // Mobile overlay
  const overlay = document.createElement('div');
  overlay.className = 'sidebar-overlay';
  overlay.id = 'sidebar-overlay';
  overlay.addEventListener('click', () => {
    sidebar.classList.remove('open');
    overlay.classList.remove('visible');
  });
  container.appendChild(overlay);

  // Mobile menu button
  const menuBtn = document.createElement('button');
  menuBtn.className = 'mobile-menu-btn';
  menuBtn.id = 'mobile-menu-btn';
  menuBtn.innerHTML = 'â˜°';
  menuBtn.addEventListener('click', () => {
    sidebar.classList.toggle('open');
    overlay.classList.toggle('visible');
  });
  container.appendChild(menuBtn);
}


=== js/components/subjectDetail.js ===

// ============================================
// DHRYZN â€” Subject Detail Component
// ============================================

import { staggerReveal } from '../utils/animations.js';

export function renderSubjectDetail(container, params, store, router) {
  const subjectId = params[0];
  const subjects = store.get('subjects');
  const subject = subjects.find(s => s.id === subjectId);

  if (!subject) {
    container.innerHTML = `
      <div class="page-container">
        <div class="empty-state">
          <div class="empty-state-icon">ðŸ”</div>
          <h2 class="empty-state-title">Subject Not Found</h2>
          <p class="empty-state-text">This subject doesn't exist.</p>
          <button class="btn btn-primary" style="margin-top: 20px;" onclick="location.hash='subjects'">Back to Subjects</button>
        </div>
      </div>
    `;
    return;
  }

  const quizHistory = store.get('quizHistory').filter(q => q.subject === subject.name);
  const examHistory = store.get('examHistory').filter(e => e.subject === subject.name);

  container.innerHTML = `
    <div class="page-container">
      <div class="subject-detail-header">
        <button class="subject-detail-back" id="back-btn">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
        </button>
        <div class="subject-detail-info">
          <h1>${subject.icon} ${subject.name}</h1>
          <p>${subject.questionsAnswered} questions answered â€¢ ${quizHistory.length} quizzes â€¢ ${examHistory.length} exams</p>
        </div>
      </div>

      <div class="subject-actions">
        <div class="subject-action-card animate-item" data-action="quiz">
          <div class="subject-action-icon" style="background: rgba(124, 92, 255, 0.12); color: #A78BFA;">ðŸ“</div>
          <div class="subject-action-title">Generate Quiz</div>
          <div class="subject-action-desc">Create a custom quiz for ${subject.name}</div>
        </div>
        <div class="subject-action-card animate-item" data-action="explain">
          <div class="subject-action-icon" style="background: rgba(96, 165, 250, 0.12); color: #60A5FA;">ðŸ’¡</div>
          <div class="subject-action-title">Explain Topic</div>
          <div class="subject-action-desc">Get detailed explanations</div>
        </div>
        <div class="subject-action-card animate-item" data-action="exam">
          <div class="subject-action-icon" style="background: rgba(251, 191, 36, 0.12); color: #FBBF24;">ðŸŽ¯</div>
          <div class="subject-action-title">Practice Exam</div>
          <div class="subject-action-desc">Simulate a real exam</div>
        </div>
        <div class="subject-action-card animate-item" data-action="results">
          <div class="subject-action-icon" style="background: rgba(52, 211, 153, 0.12); color: #34D399;">ðŸ“Š</div>
          <div class="subject-action-title">Past Results</div>
          <div class="subject-action-desc">View your performance history</div>
        </div>
      </div>

      ${quizHistory.length > 0 ? `
        <h2 class="text-h2" style="margin-bottom: var(--space-4);">Recent Activity</h2>
        <div class="history-list">
          ${quizHistory.slice(0, 3).map(q => `
            <div class="history-card">
              <div class="history-card-icon quiz">ðŸ“</div>
              <div class="history-card-info">
                <div class="history-card-title">${q.topic}</div>
                <div class="history-card-meta">
                  <span>ðŸ“… ${q.date}</span>
                  <span>â± ${q.duration}</span>
                </div>
              </div>
              <div class="history-card-score ${q.score >= 80 ? 'high' : q.score >= 60 ? 'medium' : 'low'}">${q.score}%</div>
            </div>
          `).join('')}
        </div>
      ` : ''}
    </div>
  `;

  staggerReveal(container, '.animate-item', 80);

  // Back button
  container.querySelector('#back-btn').addEventListener('click', () => {
    router.navigate('subjects');
  });

  // Action handlers
  container.querySelectorAll('.subject-action-card').forEach(card => {
    card.addEventListener('click', () => {
      const action = card.dataset.action;
      switch (action) {
        case 'quiz': router.navigate(`quiz/${subjectId}`); break;
        case 'explain': router.navigate(`explain/${subjectId}`); break;
        case 'exam': router.navigate(`exam/${subjectId}`); break;
        case 'results': router.navigate('history'); break;
      }
    });
  });
}


=== js/components/subjects.js ===

// ============================================
// DHRYZN â€” Subjects Component
// ============================================

import { staggerReveal } from '../utils/animations.js';

export function renderSubjects(container, params, store, router) {
  const subjects = store.get('subjects');

  container.innerHTML = `
    <div class="page-container">
      <div class="subjects-header">
        <h1>ðŸ“š Subjects</h1>
        <p>Choose a subject to start learning, practicing, or testing.</p>
      </div>

      <div class="subjects-grid">
        ${subjects.map(subject => `
          <div class="subject-card card-interactive animate-item" data-id="${subject.id}">
            <div class="subject-icon" style="background: ${subject.color}15; color: ${subject.color};">${subject.icon}</div>
            <div class="subject-name">${subject.name}</div>
            <div class="subject-meta">
              <span>ðŸ“ ${subject.questionsAnswered} questions answered</span>
            </div>
          </div>
        `).join('')}

        <div class="subject-card add-card card-interactive animate-item" id="add-subject-btn">
          <div class="add-card-icon">+</div>
          <div class="add-card-text">Add Custom Subject</div>
        </div>
      </div>

      <!-- Add Subject Modal (hidden) -->
      <div class="modal-overlay" id="add-subject-modal" style="display: none;">
        <div class="modal">
          <div class="modal-header">
            <h2 class="modal-title">Add Custom Subject</h2>
            <button class="modal-close" id="modal-close-btn">âœ•</button>
          </div>
          <div class="subject-modal-input">
            <label>Subject Name</label>
            <input type="text" class="input-field" id="new-subject-name" placeholder="e.g., Economics">
          </div>
          <div class="subject-modal-input">
            <label>Icon (emoji)</label>
            <input type="text" class="input-field" id="new-subject-icon" placeholder="e.g., ðŸ“Š" maxlength="4">
          </div>
          <div class="subject-modal-actions">
            <button class="btn btn-secondary" id="modal-cancel-btn">Cancel</button>
            <button class="btn btn-primary" id="modal-save-btn">Add Subject</button>
          </div>
        </div>
      </div>
    </div>
  `;

  staggerReveal(container, '.animate-item', 50);

  // Subject card click
  container.querySelectorAll('.subject-card:not(.add-card)').forEach(card => {
    card.addEventListener('click', () => {
      const id = card.dataset.id;
      router.navigate(`subject/${id}`);
    });
  });

  // Add subject modal
  const modal = container.querySelector('#add-subject-modal');
  const addBtn = container.querySelector('#add-subject-btn');
  const closeBtn = container.querySelector('#modal-close-btn');
  const cancelBtn = container.querySelector('#modal-cancel-btn');
  const saveBtn = container.querySelector('#modal-save-btn');
  const nameInput = container.querySelector('#new-subject-name');
  const iconInput = container.querySelector('#new-subject-icon');

  function openModal() { modal.style.display = 'flex'; }
  function closeModal() { modal.style.display = 'none'; nameInput.value = ''; iconInput.value = ''; }

  addBtn.addEventListener('click', openModal);
  closeBtn.addEventListener('click', closeModal);
  cancelBtn.addEventListener('click', closeModal);
  modal.addEventListener('click', (e) => { if (e.target === modal) closeModal(); });

  saveBtn.addEventListener('click', () => {
    const name = nameInput.value.trim();
    const icon = iconInput.value.trim() || 'ðŸ“„';
    if (!name) return;

    const id = name.toLowerCase().replace(/\s+/g, '-');
    const colors = ['#7C5CFF', '#60A5FA', '#34D399', '#F87171', '#FBBF24', '#A78BFA', '#FB923C'];
    const color = colors[Math.floor(Math.random() * colors.length)];

    store.addSubject({ id, name, icon, color, questionsAnswered: 0 });
    closeModal();
    renderSubjects(container, params, store, router); // Re-render
  });
}


=== js/data/sampleData.js ===

// ============================================
// DHRYZN â€” Sample Data
// ============================================

export const subjectIcons = {
  'Mathematics': 'ðŸ“',
  'Physics': 'âš›ï¸',
  'Chemistry': 'ðŸ§ª',
  'Biology': 'ðŸ§¬',
  'English': 'ðŸ“–',
  'Computer Science': 'ðŸ’»',
  'History': 'ðŸ›ï¸'
};

export const subjectColors = {
  'Mathematics': '#7C5CFF',
  'Physics': '#60A5FA',
  'Chemistry': '#34D399',
  'Biology': '#F87171',
  'English': '#FBBF24',
  'Computer Science': '#A78BFA',
  'History': '#FB923C'
};

export const weekDays = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];

export const difficultyLevels = ['Easy', 'Medium', 'Hard'];

export const gradeLevels = [
  'Grade 9', 'Grade 10', 'Grade 11', 'Grade 12',
  'Freshman', 'Sophomore', 'Junior', 'Senior',
  'Self-Learner'
];

export const questionTypes = [
  'Multiple Choice',
  'True / False',
  'Short Answer',
  'Essay'
];

export const explanationStyles = [
  'Quick Summary',
  'Step-by-Step',
  'Detailed',
  'With Examples'
];

export const suggestedPrompts = [
  "Explain quantum physics simply",
  "Help me with calculus integration",
  "What are Newton's laws?",
  "Quiz me on chemical bonding"
];

export const accentColors = [
  { name: 'Purple', value: '#7C5CFF' },
  { name: 'Blue', value: '#60A5FA' },
  { name: 'Teal', value: '#2DD4BF' },
  { name: 'Green', value: '#34D399' },
  { name: 'Rose', value: '#FB7185' },
  { name: 'Amber', value: '#FBBF24' }
];


=== js/utils/animations.js ===

// ============================================
// DHRYZN â€” Animation Utilities
// ============================================

export function initScrollAnimations(container) {
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );

  const elements = container.querySelectorAll('.animate-on-scroll');
  elements.forEach(el => observer.observe(el));

  return () => observer.disconnect();
}

export function staggerReveal(container, selector, delay = 60) {
  const elements = container.querySelectorAll(selector);
  elements.forEach((el, i) => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(16px)';
    el.style.transition = `opacity 0.4s ease ${i * delay}ms, transform 0.4s ease ${i * delay}ms`;
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        el.style.opacity = '1';
        el.style.transform = 'translateY(0)';
      });
    });
  });
}

export function typewriterEffect(element, text, speed = 18) {
  return new Promise(resolve => {
    let i = 0;
    element.textContent = '';
    function type() {
      if (i < text.length) {
        element.textContent += text[i];
        i++;
        setTimeout(type, speed);
      } else {
        resolve();
      }
    }
    type();
  });
}

export function countUpAnimation(element, target, duration = 1000, suffix = '') {
  const start = 0;
  const startTime = performance.now();

  function update(currentTime) {
    const elapsed = currentTime - startTime;
    const progress = Math.min(elapsed / duration, 1);
    const eased = 1 - Math.pow(1 - progress, 3); // ease-out cubic
    const current = Math.round(start + (target - start) * eased);
    element.textContent = current + suffix;

    if (progress < 1) {
      requestAnimationFrame(update);
    }
  }

  requestAnimationFrame(update);
}


=== js/utils/charts.js ===

// ============================================
// DHRYZN â€” Custom Canvas Charts
// ============================================

const PRIMARY = '#7C5CFF';
const PRIMARY_LIGHT = '#A78BFA';
const SUCCESS = '#34D399';
const WARNING = '#FBBF24';
const ERROR = '#F87171';
const SURFACE = '#1E1E32';
const TEXT_SEC = '#A9A9B2';
const BORDER = 'rgba(124, 92, 255, 0.12)';

function setupCanvas(canvas, width, height) {
  const dpr = window.devicePixelRatio || 1;
  canvas.width = width * dpr;
  canvas.height = height * dpr;
  canvas.style.width = width + 'px';
  canvas.style.height = height + 'px';
  const ctx = canvas.getContext('2d');
  ctx.scale(dpr, dpr);
  return ctx;
}

// ---- Bar Chart ----
export function drawBarChart(canvas, data, labels, options = {}) {
  const w = options.width || 480;
  const h = options.height || 200;
  const ctx = setupCanvas(canvas, w, h);
  const padding = { top: 20, right: 20, bottom: 40, left: 40 };
  const chartW = w - padding.left - padding.right;
  const chartH = h - padding.top - padding.bottom;
  const max = Math.max(...data, 1);
  const barWidth = Math.min(36, (chartW / data.length) * 0.6);
  const gap = (chartW - barWidth * data.length) / (data.length + 1);

  // Grid lines
  ctx.strokeStyle = BORDER;
  ctx.lineWidth = 1;
  for (let i = 0; i <= 4; i++) {
    const y = padding.top + (chartH / 4) * i;
    ctx.beginPath();
    ctx.moveTo(padding.left, y);
    ctx.lineTo(w - padding.right, y);
    ctx.stroke();
  }

  // Bars
  data.forEach((val, i) => {
    const x = padding.left + gap + i * (barWidth + gap);
    const barH = (val / max) * chartH;
    const y = padding.top + chartH - barH;

    // Bar gradient
    const grad = ctx.createLinearGradient(x, y, x, y + barH);
    grad.addColorStop(0, PRIMARY);
    grad.addColorStop(1, 'rgba(124, 92, 255, 0.4)');
    ctx.fillStyle = grad;

    // Rounded top
    const r = Math.min(4, barWidth / 2);
    ctx.beginPath();
    ctx.moveTo(x, y + barH);
    ctx.lineTo(x, y + r);
    ctx.quadraticCurveTo(x, y, x + r, y);
    ctx.lineTo(x + barWidth - r, y);
    ctx.quadraticCurveTo(x + barWidth, y, x + barWidth, y + r);
    ctx.lineTo(x + barWidth, y + barH);
    ctx.closePath();
    ctx.fill();

    // Label
    ctx.fillStyle = TEXT_SEC;
    ctx.font = '11px Inter, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText(labels[i] || '', x + barWidth / 2, h - padding.bottom + 18);
  });

  // Y-axis labels
  ctx.fillStyle = TEXT_SEC;
  ctx.font = '10px Inter, sans-serif';
  ctx.textAlign = 'right';
  for (let i = 0; i <= 4; i++) {
    const val = Math.round(max - (max / 4) * i);
    const y = padding.top + (chartH / 4) * i;
    ctx.fillText(val, padding.left - 8, y + 4);
  }
}

// ---- Line Chart ----
export function drawLineChart(canvas, data, labels, options = {}) {
  const w = options.width || 480;
  const h = options.height || 200;
  const ctx = setupCanvas(canvas, w, h);
  const padding = { top: 20, right: 20, bottom: 40, left: 40 };
  const chartW = w - padding.left - padding.right;
  const chartH = h - padding.top - padding.bottom;
  const max = Math.max(...data, 1);
  const min = Math.min(...data, 0);
  const range = max - min || 1;

  // Grid lines
  ctx.strokeStyle = BORDER;
  ctx.lineWidth = 1;
  for (let i = 0; i <= 4; i++) {
    const y = padding.top + (chartH / 4) * i;
    ctx.beginPath();
    ctx.moveTo(padding.left, y);
    ctx.lineTo(w - padding.right, y);
    ctx.stroke();
  }

  // Line
  const points = data.map((val, i) => ({
    x: padding.left + (chartW / (data.length - 1)) * i,
    y: padding.top + chartH - ((val - min) / range) * chartH
  }));

  // Area gradient
  const grad = ctx.createLinearGradient(0, padding.top, 0, h - padding.bottom);
  grad.addColorStop(0, 'rgba(124, 92, 255, 0.2)');
  grad.addColorStop(1, 'rgba(124, 92, 255, 0)');

  ctx.beginPath();
  ctx.moveTo(points[0].x, h - padding.bottom);
  points.forEach((p, i) => {
    if (i === 0) ctx.lineTo(p.x, p.y);
    else {
      const prev = points[i - 1];
      const cpx = (prev.x + p.x) / 2;
      ctx.bezierCurveTo(cpx, prev.y, cpx, p.y, p.x, p.y);
    }
  });
  ctx.lineTo(points[points.length - 1].x, h - padding.bottom);
  ctx.closePath();
  ctx.fillStyle = grad;
  ctx.fill();

  // Line stroke
  ctx.beginPath();
  points.forEach((p, i) => {
    if (i === 0) ctx.moveTo(p.x, p.y);
    else {
      const prev = points[i - 1];
      const cpx = (prev.x + p.x) / 2;
      ctx.bezierCurveTo(cpx, prev.y, cpx, p.y, p.x, p.y);
    }
  });
  ctx.strokeStyle = PRIMARY;
  ctx.lineWidth = 2.5;
  ctx.stroke();

  // Points
  points.forEach(p => {
    ctx.beginPath();
    ctx.arc(p.x, p.y, 4, 0, Math.PI * 2);
    ctx.fillStyle = PRIMARY;
    ctx.fill();
    ctx.beginPath();
    ctx.arc(p.x, p.y, 2, 0, Math.PI * 2);
    ctx.fillStyle = '#fff';
    ctx.fill();
  });

  // Labels
  ctx.fillStyle = TEXT_SEC;
  ctx.font = '11px Inter, sans-serif';
  ctx.textAlign = 'center';
  labels.forEach((label, i) => {
    const x = padding.left + (chartW / (data.length - 1)) * i;
    ctx.fillText(label, x, h - padding.bottom + 18);
  });
}

// ---- Circular Progress / Gauge ----
export function drawCircularProgress(canvas, percent, options = {}) {
  const size = options.size || 140;
  const ctx = setupCanvas(canvas, size, size);
  const center = size / 2;
  const radius = size / 2 - 12;
  const lineWidth = options.lineWidth || 10;
  const startAngle = -Math.PI / 2;
  const endAngle = startAngle + (2 * Math.PI * percent) / 100;

  // Track
  ctx.beginPath();
  ctx.arc(center, center, radius, 0, Math.PI * 2);
  ctx.strokeStyle = 'rgba(124, 92, 255, 0.1)';
  ctx.lineWidth = lineWidth;
  ctx.lineCap = 'round';
  ctx.stroke();

  // Progress
  const grad = ctx.createLinearGradient(0, 0, size, size);
  let color1 = PRIMARY;
  let color2 = PRIMARY_LIGHT;
  if (percent >= 80) { color1 = SUCCESS; color2 = '#6EE7B7'; }
  else if (percent < 50) { color1 = ERROR; color2 = '#FCA5A5'; }
  else if (percent < 70) { color1 = WARNING; color2 = '#FDE68A'; }
  grad.addColorStop(0, color1);
  grad.addColorStop(1, color2);

  ctx.beginPath();
  ctx.arc(center, center, radius, startAngle, endAngle);
  ctx.strokeStyle = grad;
  ctx.lineWidth = lineWidth;
  ctx.lineCap = 'round';
  ctx.stroke();
}

// ---- Radial Gauge (Semi-circle) ----
export function drawRadialGauge(canvas, percent, options = {}) {
  const size = options.size || 120;
  const ctx = setupCanvas(canvas, size, size);
  const center = size / 2;
  const radius = size / 2 - 12;
  const lineWidth = options.lineWidth || 10;
  const startAngle = Math.PI;
  const totalAngle = Math.PI;
  const endAngle = startAngle + totalAngle * (percent / 100);

  // Track
  ctx.beginPath();
  ctx.arc(center, center, radius, startAngle, startAngle + totalAngle);
  ctx.strokeStyle = 'rgba(124, 92, 255, 0.1)';
  ctx.lineWidth = lineWidth;
  ctx.lineCap = 'round';
  ctx.stroke();

  // Progress
  const grad = ctx.createLinearGradient(0, 0, size, 0);
  grad.addColorStop(0, ERROR);
  grad.addColorStop(0.5, WARNING);
  grad.addColorStop(1, SUCCESS);

  ctx.beginPath();
  ctx.arc(center, center, radius, startAngle, endAngle);
  ctx.strokeStyle = grad;
  ctx.lineWidth = lineWidth;
  ctx.lineCap = 'round';
  ctx.stroke();
}


=== js/utils/greeting.js ===

// ============================================
// DHRYZN â€” Greeting Utility
// ============================================

const motivationalMessages = [
  "Ready to continue your learning journey?",
  "Let's make today count!",
  "Knowledge is your superpower.",
  "Every question you answer makes you stronger.",
  "Small steps lead to big achievements.",
  "You're doing great â€” keep going!",
  "Learning never stops. Let's go!",
  "Your future self will thank you.",
  "Consistency is the key to mastery.",
  "Let's turn curiosity into knowledge."
];

export function getGreeting() {
  const hour = new Date().getHours();
  let emoji, text;

  if (hour >= 5 && hour < 12) {
    emoji = 'ðŸŒ…';
    text = 'Good Morning';
  } else if (hour >= 12 && hour < 17) {
    emoji = 'â˜€ï¸';
    text = 'Good Afternoon';
  } else if (hour >= 17 && hour < 21) {
    emoji = 'ðŸŒ™';
    text = 'Good Evening';
  } else {
    emoji = 'ðŸŒƒ';
    text = 'Good Night';
  }

  const motivational = motivationalMessages[Math.floor(Math.random() * motivationalMessages.length)];

  return { emoji, text, motivational };
}


=== js/utils/mockAI.js ===

// ============================================
// DHRYZN â€” Mock AI Responses
// ============================================

const RESPONSE_DELAY_MIN = 800;
const RESPONSE_DELAY_MAX = 2000;

function delay(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}

function randomDelay() {
  return RESPONSE_DELAY_MIN + Math.random() * (RESPONSE_DELAY_MAX - RESPONSE_DELAY_MIN);
}

const generalResponses = [
  "Great question! Let me break this down for you in a way that's easy to understand.",
  "I'd love to help you with that! Here's what you need to know:",
  "That's a really important concept. Let me explain it step by step.",
  "Excellent curiosity! Understanding this will really strengthen your foundation.",
  "Let's dive into this together. I'll make sure it clicks for you!"
];

const topicExplanations = {
  'quadratic': `**Quadratic Equations** are polynomial equations of degree 2, written in the form:

**axÂ² + bx + c = 0**

Where a, b, and c are constants, and a â‰  0.

**Key Methods to Solve:**
1. **Factoring** â€” Break the equation into two binomial factors
2. **Quadratic Formula** â€” x = (-b Â± âˆš(bÂ²-4ac)) / 2a
3. **Completing the Square** â€” Rewrite in vertex form

**The Discriminant (bÂ²-4ac)** tells us about the solutions:
â€¢ Positive â†’ Two real solutions
â€¢ Zero â†’ One real solution
â€¢ Negative â†’ Two complex solutions

Would you like me to generate a practice quiz on this topic? ðŸ“`,

  'newton': `**Newton's Laws of Motion** are three fundamental principles that describe how objects move:

**1st Law (Inertia):** An object at rest stays at rest, and an object in motion stays in motion unless acted upon by an external force.

**2nd Law (F = ma):** The acceleration of an object equals the net force acting on it divided by its mass. This is the most used law in problem-solving!

**3rd Law (Action-Reaction):** For every action, there is an equal and opposite reaction.

**Pro Tip:** Most physics problems use the 2nd Law. Start by drawing a free-body diagram and identifying all forces! ðŸ’¡

Ready to test your understanding? I can create a quick quiz for you! ðŸŽ¯`,

  'default': `That's a wonderful topic to explore! Here's a clear breakdown:

**Key Concepts:**
â€¢ Start with the fundamentals â€” make sure your foundation is solid
â€¢ Connect new ideas to what you already know
â€¢ Practice with examples to reinforce understanding

**Study Strategy:**
1. Read through the core theory first
2. Work through guided examples
3. Try practice problems independently
4. Review mistakes and identify patterns

I'm here to help you every step of the way! Want me to generate a quiz to test your understanding? ðŸ“`
};

export async function getAIResponse(message) {
  await delay(randomDelay());

  const lower = message.toLowerCase();

  // Check for topic-specific responses
  if (lower.includes('quadratic') || lower.includes('equation')) {
    return topicExplanations['quadratic'];
  }
  if (lower.includes('newton') || lower.includes('motion') || lower.includes('force')) {
    return topicExplanations['newton'];
  }

  // Check for action intents
  if (lower.includes('quiz') || lower.includes('test me') || lower.includes('practice')) {
    return `Absolutely! I'd love to help you practice! ðŸŽ¯\n\nTo create the perfect quiz for you, I need a few details:\n\n1. **Subject** â€” What topic would you like to focus on?\n2. **Difficulty** â€” Easy, Medium, or Hard?\n3. **Question Count** â€” How many questions?\n\nOr you can use the **Generate Quiz** feature from the Quick Actions above for a more customized experience! You'll be able to choose question types, upload notes, and more.`;
  }

  if (lower.includes('explain') || lower.includes('what is') || lower.includes('how does') || lower.includes('tell me about')) {
    return topicExplanations['default'];
  }

  if (lower.includes('exam') || lower.includes('prepare') || lower.includes('ready')) {
    return `Let's get you exam-ready! ðŸ’ª\n\nHere's my recommended study plan:\n\n**1. Review Core Concepts** â€” Make sure you understand the fundamentals\n**2. Practice with Quizzes** â€” Use our quiz feature for active recall\n**3. Take a Practice Exam** â€” Simulate real exam conditions\n**4. Analyze Results** â€” Focus on your weak areas\n**5. Repeat** â€” Practice makes permanent!\n\nWant me to start with a practice exam simulation? You can access it from the Quick Actions or the Subjects page.`;
  }

  if (lower.includes('help') || lower.includes('what can you do')) {
    return `I'm your personal AI Study Mentor! Here's what I can do for you: ðŸŒŸ\n\nðŸ“š **Explain Topics** â€” Clear, step-by-step explanations\nðŸ“ **Generate Quizzes** â€” Custom quizzes with instant feedback\nðŸŽ¯ **Practice Exams** â€” Realistic exam simulations\nðŸ“Š **Track Progress** â€” Monitor your learning journey\nðŸ’¡ **Recommend** â€” Suggest what to study next\n\nJust type your question or use the Quick Actions to get started!`;
  }

  if (lower.includes('hello') || lower.includes('hi') || lower.includes('hey')) {
    return `Hey there! ðŸ‘‹ Great to see you! I'm DHRYZN, your personal study mentor.\n\nI'm here to help you learn, practice, and grow. What would you like to work on today?\n\nðŸ’¡ **Tip:** You can ask me to explain any topic, generate a quiz, or help you prepare for an exam!`;
  }

  if (lower.includes('thank')) {
    return `You're very welcome! ðŸ˜Š Remember, every question you ask makes you a better learner. I'm always here whenever you need help.\n\nKeep up the great work! ðŸŒŸ`;
  }

  // Default response
  const random = generalResponses[Math.floor(Math.random() * generalResponses.length)];
  return `${random}\n\nI can help you explore this further! Here are some options:\n\nâ€¢ Ask me to **explain** any topic in detail\nâ€¢ Let me **generate a quiz** to test your knowledge\nâ€¢ Try a **practice exam** to simulate real conditions\n\nWhat would you like to do? ðŸŽ¯`;
}

// ---- Quiz Generation ----
export function generateQuizQuestions(topic, count, type, difficulty) {
  const questions = getQuestionBank(topic, type);
  const selected = questions.slice(0, Math.min(count, questions.length));

  if (selected.length < count) {
    // Pad with generated questions
    for (let i = selected.length; i < count; i++) {
      selected.push(generateFallbackQuestion(topic, type, i + 1));
    }
  }

  return selected;
}

function getQuestionBank(topic, type) {
  const lower = (topic || '').toLowerCase();

  if (lower.includes('math') || lower.includes('algebra') || lower.includes('quadratic')) {
    return getMathQuestions(type);
  }
  if (lower.includes('physics') || lower.includes('newton') || lower.includes('motion')) {
    return getPhysicsQuestions(type);
  }
  if (lower.includes('chemistry') || lower.includes('chemical')) {
    return getChemistryQuestions(type);
  }
  if (lower.includes('biology') || lower.includes('cell') || lower.includes('dna')) {
    return getBiologyQuestions(type);
  }
  if (lower.includes('computer') || lower.includes('programming') || lower.includes('data structure')) {
    return getCSQuestions(type);
  }

  return getGeneralQuestions(topic, type);
}

function getMathQuestions(type) {
  if (type === 'True / False') {
    return [
      { question: 'The quadratic formula can solve any quadratic equation.', answer: true, explanation: 'The quadratic formula x = (-b Â± âˆš(bÂ²-4ac)) / 2a works for all quadratic equations axÂ² + bx + c = 0 where a â‰  0.' },
      { question: 'A quadratic equation always has two real roots.', answer: false, explanation: 'A quadratic equation can have two real roots, one repeated root, or two complex roots depending on the discriminant (bÂ²-4ac).' },
      { question: 'The graph of a quadratic function is called a parabola.', answer: true, explanation: 'Correct! All quadratic functions y = axÂ² + bx + c produce parabolic curves.' },
      { question: 'If the discriminant is negative, the equation has no solutions.', answer: false, explanation: 'If the discriminant is negative, the equation has two complex (imaginary) solutions, not zero solutions.' },
      { question: 'The vertex of y = xÂ² - 4x + 3 is at (2, -1).', answer: true, explanation: 'Using x = -b/2a = 4/2 = 2, and y = 4 - 8 + 3 = -1. So the vertex is at (2, -1).' }
    ];
  }
  return [
    { question: 'What is the quadratic formula?', options: ['x = (-b Â± âˆš(bÂ²-4ac)) / 2a', 'x = -b / 2a', 'x = (-b Â± âˆš(bÂ²+4ac)) / 2a', 'x = bÂ² - 4ac'], correct: 0, explanation: 'The quadratic formula is x = (-b Â± âˆš(bÂ²-4ac)) / 2a, derived from completing the square on axÂ² + bx + c = 0.' },
    { question: 'If bÂ² - 4ac = 0, how many real roots does the equation have?', options: ['No real roots', 'One repeated root', 'Two distinct roots', 'Infinite roots'], correct: 1, explanation: 'When the discriminant equals zero, the equation has exactly one repeated (double) root.' },
    { question: 'What is the sum of roots of axÂ² + bx + c = 0?', options: ['b/a', '-b/a', 'c/a', '-c/a'], correct: 1, explanation: 'By Vieta\'s formulas, the sum of roots = -b/a.' },
    { question: 'Solve: xÂ² - 5x + 6 = 0', options: ['x = 2, x = 3', 'x = -2, x = -3', 'x = 1, x = 6', 'x = -1, x = -6'], correct: 0, explanation: 'Factoring: (x-2)(x-3) = 0, so x = 2 or x = 3.' },
    { question: 'What shape does a quadratic function graph?', options: ['Line', 'Parabola', 'Circle', 'Hyperbola'], correct: 1, explanation: 'A quadratic function always graphs as a parabola â€” a U-shaped curve that opens up or down.' },
    { question: 'What is the product of roots of axÂ² + bx + c = 0?', options: ['b/a', '-b/a', 'c/a', '-c/a'], correct: 2, explanation: 'By Vieta\'s formulas, the product of roots = c/a.' },
    { question: 'The vertex form of a quadratic is:', options: ['y = axÂ² + bx + c', 'y = a(x-h)Â² + k', 'y = a(x-râ‚)(x-râ‚‚)', 'y = mx + b'], correct: 1, explanation: 'Vertex form is y = a(x-h)Â² + k where (h,k) is the vertex of the parabola.' },
    { question: 'For f(x) = 2xÂ² - 8x + 6, find the axis of symmetry.', options: ['x = 2', 'x = -2', 'x = 4', 'x = -4'], correct: 0, explanation: 'Axis of symmetry = -b/(2a) = 8/(2Ã—2) = 2.' },
    { question: 'Which value of c makes xÂ² + 6x + c a perfect square?', options: ['6', '9', '12', '36'], correct: 1, explanation: 'For a perfect square: c = (b/2)Â² = (6/2)Â² = 9. This gives (x+3)Â².' },
    { question: 'If one root of xÂ² - 7x + k = 0 is 3, find k.', options: ['10', '12', '15', '21'], correct: 1, explanation: 'If x=3 is a root: 9 - 21 + k = 0, so k = 12.' }
  ];
}

function getPhysicsQuestions(type) {
  if (type === 'True / False') {
    return [
      { question: 'Newton\'s First Law is also known as the Law of Inertia.', answer: true, explanation: 'Correct! The First Law states that an object maintains its state of motion unless acted upon by an external force â€” this property is called inertia.' },
      { question: 'Mass and weight are the same thing.', answer: false, explanation: 'Mass is a measure of matter (kg), while weight is the gravitational force on that mass (W = mg, measured in Newtons).' },
      { question: 'According to Newton\'s Third Law, forces always come in pairs.', answer: true, explanation: 'Every action has an equal and opposite reaction â€” forces always occur in action-reaction pairs.' },
      { question: 'An object can accelerate even if no force acts on it.', answer: false, explanation: 'By Newton\'s Second Law (F=ma), acceleration requires a net force. No force means no acceleration.' },
      { question: 'Friction always opposes the direction of motion.', answer: true, explanation: 'Friction is a force that opposes relative motion between surfaces in contact.' }
    ];
  }
  return [
    { question: 'What is Newton\'s Second Law of Motion?', options: ['F = ma', 'F = mv', 'F = m/a', 'F = mg'], correct: 0, explanation: 'Newton\'s Second Law states that Force equals mass times acceleration: F = ma.' },
    { question: 'A 5 kg object accelerates at 3 m/sÂ². What is the net force?', options: ['8 N', '15 N', '1.67 N', '2 N'], correct: 1, explanation: 'F = ma = 5 Ã— 3 = 15 N.' },
    { question: 'What happens to acceleration if force is doubled and mass stays constant?', options: ['Halved', 'Unchanged', 'Doubled', 'Quadrupled'], correct: 2, explanation: 'From F = ma, if F doubles and m is constant, then a must also double.' },
    { question: 'Which law explains why a seatbelt is important?', options: ['1st Law', '2nd Law', '3rd Law', 'Law of Gravitation'], correct: 0, explanation: 'Newton\'s 1st Law (inertia) â€” your body tends to continue moving forward when the car stops suddenly.' },
    { question: 'What is the SI unit of force?', options: ['Joule', 'Watt', 'Newton', 'Pascal'], correct: 2, explanation: 'The SI unit of force is the Newton (N), where 1 N = 1 kgÂ·m/sÂ².' },
    { question: 'A rocket propels forward by expelling gas backward. Which law explains this?', options: ['1st Law', '2nd Law', '3rd Law', 'Law of Conservation'], correct: 2, explanation: 'Newton\'s 3rd Law â€” the rocket pushes gas backward (action), and the gas pushes the rocket forward (reaction).' },
    { question: 'What is the weight of a 10 kg object on Earth (g = 9.8 m/sÂ²)?', options: ['10 N', '98 N', '9.8 N', '100 N'], correct: 1, explanation: 'Weight = mg = 10 Ã— 9.8 = 98 N.' },
    { question: 'An object at rest has a net force of:', options: ['Maximum', 'Minimum', 'Zero', 'Undefined'], correct: 2, explanation: 'An object at rest (in equilibrium) has zero net force acting on it.' }
  ];
}

function getChemistryQuestions(type) {
  if (type === 'True / False') {
    return [
      { question: 'Ionic bonds form between metals and non-metals.', answer: true, explanation: 'Ionic bonds typically form when a metal transfers electrons to a non-metal.' },
      { question: 'Covalent bonds involve the transfer of electrons.', answer: false, explanation: 'Covalent bonds involve the sharing of electrons, not transfer. Ionic bonds involve electron transfer.' },
      { question: 'Water (Hâ‚‚O) is a polar molecule.', answer: true, explanation: 'Water is polar because of its bent shape and the electronegativity difference between O and H atoms.' },
      { question: 'Noble gases readily form chemical bonds.', answer: false, explanation: 'Noble gases have full outer electron shells, making them very stable and unreactive.' },
      { question: 'An atom with more protons than electrons is a cation.', answer: true, explanation: 'A cation is a positively charged ion formed when an atom loses electrons.' }
    ];
  }
  return [
    { question: 'What type of bond forms between Na and Cl?', options: ['Covalent', 'Ionic', 'Metallic', 'Hydrogen'], correct: 1, explanation: 'Na (metal) transfers an electron to Cl (non-metal), forming an ionic bond in NaCl.' },
    { question: 'How many covalent bonds does Carbon typically form?', options: ['2', '3', '4', '6'], correct: 2, explanation: 'Carbon has 4 valence electrons and typically forms 4 covalent bonds.' },
    { question: 'What determines an element\'s chemical properties?', options: ['Number of protons', 'Atomic mass', 'Number of valence electrons', 'Number of neutrons'], correct: 2, explanation: 'Valence electrons determine how an element bonds and reacts with other elements.' },
    { question: 'Which molecule is nonpolar?', options: ['Hâ‚‚O', 'NHâ‚ƒ', 'COâ‚‚', 'HCl'], correct: 2, explanation: 'COâ‚‚ is linear and symmetrical, so the bond dipoles cancel out, making it nonpolar.' },
    { question: 'What is the shape of a methane (CHâ‚„) molecule?', options: ['Linear', 'Trigonal planar', 'Tetrahedral', 'Octahedral'], correct: 2, explanation: 'Methane has 4 bonding pairs around carbon, giving it a tetrahedral geometry.' }
  ];
}

function getBiologyQuestions(type) {
  if (type === 'True / False') {
    return [
      { question: 'DNA stands for deoxyribonucleic acid.', answer: true, explanation: 'DNA is indeed deoxyribonucleic acid â€” the molecule that carries genetic information.' },
      { question: 'All cells have a nucleus.', answer: false, explanation: 'Prokaryotic cells (like bacteria) do not have a membrane-bound nucleus.' },
      { question: 'Mitochondria are known as the powerhouse of the cell.', answer: true, explanation: 'Mitochondria produce ATP through cellular respiration, providing energy for the cell.' },
      { question: 'Photosynthesis occurs in the mitochondria.', answer: false, explanation: 'Photosynthesis occurs in chloroplasts, not mitochondria.' },
      { question: 'RNA is typically single-stranded.', answer: true, explanation: 'Unlike DNA which is double-stranded, RNA is usually single-stranded.' }
    ];
  }
  return [
    { question: 'What is the basic unit of life?', options: ['Atom', 'Molecule', 'Cell', 'Organ'], correct: 2, explanation: 'The cell is the basic structural and functional unit of all living organisms.' },
    { question: 'Where does DNA replication occur in eukaryotes?', options: ['Cytoplasm', 'Nucleus', 'Ribosome', 'Cell membrane'], correct: 1, explanation: 'In eukaryotic cells, DNA replication occurs in the nucleus.' },
    { question: 'What is the primary function of ribosomes?', options: ['DNA replication', 'Protein synthesis', 'Cell division', 'Energy production'], correct: 1, explanation: 'Ribosomes translate mRNA into proteins through the process of translation.' },
    { question: 'Which base pairs with Adenine in DNA?', options: ['Cytosine', 'Guanine', 'Thymine', 'Uracil'], correct: 2, explanation: 'In DNA, Adenine (A) always pairs with Thymine (T) through hydrogen bonds.' },
    { question: 'What stage of mitosis do chromosomes align at the cell equator?', options: ['Prophase', 'Metaphase', 'Anaphase', 'Telophase'], correct: 1, explanation: 'During metaphase, chromosomes line up at the metaphase plate (cell equator).' }
  ];
}

function getCSQuestions(type) {
  if (type === 'True / False') {
    return [
      { question: 'An array has O(1) time complexity for accessing elements by index.', answer: true, explanation: 'Arrays provide constant-time access because elements are stored in contiguous memory locations.' },
      { question: 'A stack follows FIFO (First In, First Out) order.', answer: false, explanation: 'A stack follows LIFO (Last In, First Out). A queue follows FIFO.' },
      { question: 'Binary search requires a sorted array.', answer: true, explanation: 'Binary search repeatedly divides the search interval in half, which requires the array to be sorted.' },
      { question: 'Linked lists provide O(1) access to any element.', answer: false, explanation: 'Linked lists require O(n) traversal to access an element. Only arrays provide O(1) random access.' },
      { question: 'The time complexity of merge sort is O(n log n).', answer: true, explanation: 'Merge sort consistently performs in O(n log n) time in all cases (best, average, worst).' }
    ];
  }
  return [
    { question: 'What is the time complexity of binary search?', options: ['O(1)', 'O(n)', 'O(log n)', 'O(nÂ²)'], correct: 2, explanation: 'Binary search halves the search space each iteration, giving O(log n) complexity.' },
    { question: 'Which data structure uses LIFO ordering?', options: ['Queue', 'Stack', 'Array', 'Linked List'], correct: 1, explanation: 'A Stack follows Last-In-First-Out (LIFO) â€” the most recently added element is removed first.' },
    { question: 'What is the worst-case time complexity of quicksort?', options: ['O(n)', 'O(n log n)', 'O(nÂ²)', 'O(log n)'], correct: 2, explanation: 'Quicksort degrades to O(nÂ²) when the pivot selection is poor (e.g., already sorted array with first element as pivot).' },
    { question: 'Which traversal visits the root first in a binary tree?', options: ['In-order', 'Pre-order', 'Post-order', 'Level-order'], correct: 1, explanation: 'Pre-order traversal visits: Root â†’ Left â†’ Right.' },
    { question: 'What data structure is used for BFS?', options: ['Stack', 'Queue', 'Heap', 'Hash Table'], correct: 1, explanation: 'Breadth-First Search uses a Queue to explore nodes level by level.' },
    { question: 'A hash table provides average-case _____ lookup time.', options: ['O(1)', 'O(n)', 'O(log n)', 'O(n log n)'], correct: 0, explanation: 'Hash tables provide O(1) average-case lookup using hash functions for direct addressing.' },
    { question: 'Which sorting algorithm is stable?', options: ['Quicksort', 'Heap sort', 'Merge sort', 'Selection sort'], correct: 2, explanation: 'Merge sort is a stable sorting algorithm â€” equal elements maintain their relative order.' },
    { question: 'What is the space complexity of a recursive Fibonacci implementation?', options: ['O(1)', 'O(n)', 'O(2â¿)', 'O(log n)'], correct: 1, explanation: 'The recursive call stack goes n levels deep, requiring O(n) space.' }
  ];
}

function getGeneralQuestions(topic, type) {
  if (type === 'True / False') {
    return [
      { question: `${topic || 'This subject'} is a branch of science.`, answer: true, explanation: 'This is a foundational concept in the subject area.' },
      { question: 'All theories in science are proven facts.', answer: false, explanation: 'Scientific theories are well-supported explanations, but they can be refined or replaced as new evidence emerges.' },
      { question: 'Critical thinking is important for academic success.', answer: true, explanation: 'Critical thinking helps you analyze information, solve problems, and make informed decisions.' },
      { question: 'Memorization alone is sufficient for deep understanding.', answer: false, explanation: 'Deep understanding requires connecting concepts, applying knowledge, and analyzing information â€” not just memorization.' },
      { question: 'Practice and repetition help reinforce learning.', answer: true, explanation: 'Spaced repetition and active practice are proven study techniques that enhance long-term retention.' }
    ];
  }
  return [
    { question: `What is the most effective study technique for long-term retention of ${topic || 'any subject'}?`, options: ['Re-reading notes', 'Active recall & spaced repetition', 'Highlighting text', 'Listening to lectures'], correct: 1, explanation: 'Active recall (testing yourself) combined with spaced repetition is proven to be the most effective study method.' },
    { question: 'Which cognitive skill is most important for problem-solving?', options: ['Memorization', 'Critical thinking', 'Speed reading', 'Note-taking'], correct: 1, explanation: 'Critical thinking allows you to analyze problems, evaluate options, and create effective solutions.' },
    { question: 'What is the Feynman Technique?', options: ['Speed reading method', 'Teaching a concept in simple terms to test understanding', 'Memory palace method', 'Group study technique'], correct: 1, explanation: 'The Feynman Technique involves explaining a concept in simple language â€” if you can teach it simply, you truly understand it.' },
    { question: 'How does sleep affect learning?', options: ['No effect', 'Consolidates memories', 'Reduces memory', 'Only helps physical skills'], correct: 1, explanation: 'Sleep plays a crucial role in memory consolidation â€” your brain processes and stores new information during sleep.' },
    { question: 'What is the optimal study session length before taking a break?', options: ['15 minutes', '25-30 minutes', '60 minutes', '90+ minutes'], correct: 1, explanation: 'The Pomodoro Technique recommends 25-30 minute focused sessions followed by short breaks for optimal concentration.' }
  ];
}

function generateFallbackQuestion(topic, type, num) {
  if (type === 'True / False') {
    return {
      question: `Understanding ${topic || 'this topic'} requires consistent practice and review.`,
      answer: true,
      explanation: 'Consistent practice is key to mastering any topic. Regular review helps move information from short-term to long-term memory.'
    };
  }
  return {
    question: `Which approach is best for mastering ${topic || 'this topic'}?`,
    options: ['Only reading textbooks', 'Active practice with feedback', 'Watching videos passively', 'Cramming before exams'],
    correct: 1,
    explanation: 'Active practice with feedback (like quizzes and problem-solving) is the most effective way to achieve mastery.'
  };
}

// ---- Explanation Generation ----
export function generateExplanation(topic, style) {
  const styleMap = {
    'Quick Summary': `**Quick Summary: ${topic}**\n\nThis is a fundamental concept that forms the basis for more advanced topics. The key idea is understanding the core principles and how they connect to real-world applications.\n\n**Key Takeaway:** Focus on understanding the "why" behind the concept, not just the "what."`,
    'Step-by-Step': `**Step-by-Step Guide: ${topic}**\n\n**Step 1: Foundation**\nStart by understanding the basic definitions and terminology.\n\n**Step 2: Core Concepts**\nLearn the main principles and rules that govern this topic.\n\n**Step 3: Examples**\nWork through guided examples to see concepts in action.\n\n**Step 4: Practice**\nApply what you've learned to new problems.\n\n**Step 5: Connect**\nRelate this topic to other concepts you've learned.\n\nðŸ’¡ **Pro Tip:** Don't skip steps â€” each one builds on the previous!`,
    'Detailed': `**Detailed Explanation: ${topic}**\n\n**Introduction**\nThis topic is a cornerstone of the subject, connecting multiple concepts together. Understanding it deeply will give you a significant advantage.\n\n**Theory**\nThe theoretical foundation involves several key principles that have been developed and refined over time. Each principle serves a specific purpose and contributes to the overall framework.\n\n**Applications**\nIn practice, this knowledge is applied across many domains â€” from academic problem-solving to real-world scenarios.\n\n**Common Misconceptions**\nâ€¢ Confusing related but distinct concepts\nâ€¢ Oversimplifying complex relationships\nâ€¢ Ignoring edge cases and exceptions\n\n**Advanced Connections**\nThis topic connects to several advanced areas that you'll encounter in future studies.`,
    'With Examples': `**${topic} â€” Explained with Examples**\n\n**Concept Overview**\nLet's understand this through concrete examples that make the ideas click.\n\n**Example 1: Basic**\nImagine you're starting from scratch. The simplest case shows us the fundamental pattern at work.\nâ†’ This demonstrates the core principle in its simplest form.\n\n**Example 2: Intermediate**\nNow let's add some complexity. When we combine multiple elements, we see how the rules interact.\nâ†’ Notice how the basic principle still applies, just with more variables.\n\n**Example 3: Advanced**\nIn this real-world scenario, we apply everything together. This is the kind of problem you might see on an exam.\nâ†’ The key is breaking complex problems into smaller, manageable parts.\n\n**Practice Challenge:** Try creating your own example! Teaching is the best way to learn. ðŸŽ¯`
  };

  return styleMap[style] || styleMap['Step-by-Step'];
}

